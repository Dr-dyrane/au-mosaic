# QA ledger · The Mosaic Maison

Every checkpoint verified with evidence, not opinion. Updated each pass.
Last pass: 2026-07-06.

## The owner's checklist

| # | Check | Status | Evidence |
|---|---|---|---|
| 1 | Modularity | PASS | Clean layers: data in `src/lib` (site, products, images, wa), primitives in `globals.css`, components in `src/components`, pages only compose. No page owns data or raw hex. |
| 2 | Reusability | PASS | One `ProductCard` serves the home picks and every range grid. `Section`, `CtaRow`, `Reveal`, `TileSheet`, `MosaicMark` shared across all pages. |
| 3 | Complete design system | PASS | `docs/DESIGN.md`: laws, palette, typography, motion, components, assets. All colour flows from `@theme` tokens. |
| 4 | Theme based | PASS | Dark is the default; light is one tap, saved and applied before paint. Both palettes tokenised; `color-scheme` set per theme. |
| 5 | Contrast check | PASS | Measured (WCAG): dark theme ink 17.1, dusk 7.4, mist 4.65, gold 8.0 to 1 on sand; light theme ink 16.5, dusk 6.5, mist 4.71, gold 4.62. All AA. Button label 7.7 to 1 in both themes. |
| 6 | Typography | PASS | Native serif stack (Didot, Bodoni 72, Georgia fallback) for headlines, native sans for body. Zero webfont wait. Scale documented in DESIGN.md. |
| 7 | Hover to animate | PASS | Images glide (scale 1.03, 700ms), hairline links draw themselves, card names warm to gold, nav fades, WhatsApp float lifts and settles. |
| 8 | Animate to reveal | PASS | `Reveal.tsx` IntersectionObserver: opacity, 14px rise, 0.985 scale, staggered 80 to 180ms. Ken-burns on hero and piece pages. Reduced motion respected everywhere. |
| 9 | Microinteractions | PASS | Button lift and settle, active press states, gold focus rings, brass text selection, smooth anchor scroll with scroll margins. |
| 10 | Progressive disclosure | PASS | Five pieces then "Explore the collection". Environments before materials. Piece facts below the reveal. Never five hundred. |
| 11 | Focus view | PASS | One loud thing per screen. Hero, environment, lifestyle line, and piece reveal each own their viewport. |
| 12 | Product view, Apple reveal | PASS | `/piece/[slug]`: the image is the screen. Full-bleed, no borders, no containers, words and one gold action on the image. 11 pieces prerendered. |
| 13 | Modern product pages | PASS | SSG piece pages with per-piece metadata and OG image, colourway dots, stock and factory facts, WhatsApp close. |

## Added checks

| Check | Status | Evidence |
|---|---|---|
| Visualizer surface prep | PASS | The visualizer now has a `Prep surface` control with Primer, Blur, and Original. Primer is the default so customer photos with existing tile, mosaic, or busy floor texture get a neutral ground inside the selected quad before the new mosaic is drawn. Blur keeps broad room light while softening old grout lines. Original stays available for honest comparison. The compositor now clips prep and light blending to the four-stone surface, so the room outside the chosen area stays visible and the press-and-hold Original still shows the untouched photo. In-app browser checked `/visualizer`: H1 present, Primer selected by default, prep controls visible, and no horizontal overflow. Phone viewport at 390 by 844 checked the controls fit, Primer selected, canvas width 390, and no horizontal overflow. The browser runtime did not expose canvas pixel reads, so bitmap verification was code review plus live UI state. `npx eslint src --max-warnings=0`, `npx tsc --noEmit`, `python3 scripts/theme-check.py`, `git diff --check`, glyph scan, and `npx next build` on Next 16.2.10, 56 routes, passed. |
| Visualizer autosnap | PASS | `Find surface` now runs a first-pass in-browser surface detector over uploaded and camera stills: it downscales the image, scores horizontal and vertical luminance edges, proposes a quad for pool, wall, backsplash, shower, and floor modes, then leaves the four stones editable as the correction layer. Upload and camera capture try autosnap automatically, and the user can rerun it with one quiet control. Camera open now scrolls the preview into view on compact screens. The primary nav is back to five direct items at rest: Visualizer, Tiles, Materials, Pools, Contact, with About moved into Explore. The home hero uses Next 16 `preload` for the LCP image instead of deprecated `priority` or plain fetch priority. In-app browser checked `/visualizer` desktop: H1 present, no overflow, `Find surface` returned `Surface found. Drag corners to refine.`, and nav direct count was 5. Phone at 390 by 844 checked no overflow, Visualizer first in the menu, and Find surface present. Live camera permission was not granted in automation, so the device permission prompt still needs one real phone pass. `npx eslint src --max-warnings=0`, `npx tsc --noEmit`, `python3 scripts/theme-check.py`, `git diff --check`, glyph scan, and `npx next build` on Next 16.2.10, 56 routes, passed. |
| Visualizer northstar | PASS | Nonso's northstar moved into the public architecture. `Visualizer` is now the first primary nav item and the first real destination in the phone menu, while the old Explore entry is gone. The page title is `See it in your space`, and the home hero now points first to `/visualizer`. A new first follow-up section shows the empty pool, explains the three-step path, and links to the visualizer before the older environment and catalogue sections. The visualizer gained a live camera lane: `getUserMedia` opens a rear-camera preview when allowed, `ImageCapture.takePhoto` is used when available, and canvas capture stands behind it, with upload still the fallback. `docs/NEXT-STEPS.md` now names the real engine next: Find surface with quad detection first, then MediaPipe interactive segmentation or a custom model only if geometry is not enough. In-app browser checked `/visualizer` at 1280 by 900: title, H1, canvas, `Use camera`, no console errors, and no overflow. Browser checked home desktop, home phone, phone menu, and the 1024 header edge: Visualizer leads the nav surfaces and no overflow. `npx tsc --noEmit`, `npx eslint src --max-warnings=0`, `python3 scripts/theme-check.py`, `git diff --check`, dash scan, and `npx next build` on Next 16.2.10, 56 routes, passed. |
| Action chrome stands down | PASS | The compact gold action stops faking a verb where a room has none. `adminRouteActionFor` now returns null for Insights, Settings, and an empty Owed ledger, and `AdminTabBar` renders the gold action only when one exists, so those rooms show no action instead of a Today or a History that only navigates, or a Remind that reloads the page he is already on. The debts room keeps its own Remind oldest marker, which still wins whenever a balance is open, so the owner loses nothing. The now-dead `owed` argument left `adminRouteActionFor`, `useResolvedAdminAction`, and `AdminTabBar`; `contextActionsFor` is null safe, and record pages plus the desktop context rail are unchanged. Closes back-office action audit items C and D. `npx tsc --noEmit` clean; `npx eslint src scripts drizzle.config.ts --max-warnings=0` clean; `git diff --check` clean; em dash and arrow scan clean. The production build was not run this pass: the mounted `node_modules` carries the owner's macOS binaries, which must stay intact for his own build, so the Linux build runs on the next Vercel deploy. |
| Nonso guided tour polish | PASS | Complexity and UX pass kept the live guided-tour shell but made the controller sturdier: one `state` object, grouped `ui` references, DOM rendering through `replaceChildren`, safe text assignment, hash deep links plus `hashchange`, a guarded swipe path, and `aria-pressed` plus `aria-current` on active controls. Mobile now hides the device picker and live label, keeps Guide or Drive as the only stage control, snaps the path, and expands only the active chapter label. In-app browser read smoke on `#piece` showed Piece active, phone device, and the right story. Chrome CDP then verified desktop home, `#visualizer`, dark plus Lagoon persistence inside iframe storage, and `#piece` at 390 by 844 with no horizontal overflow and the active chapter fully visible. Screenshots saved to `/tmp/au-mosaic-qa/unveiling-pass-desktop.png`, `/tmp/au-mosaic-qa/unveiling-pass-visualizer.png`, and `/tmp/au-mosaic-qa/unveiling-pass-mobile.png`. Source scan found no em or en dash glyphs, no off-ramp `font-size` values, no string-built DOM writes, and no broad `data-*` selector handlers. Inline script syntax extraction, `git diff --check -- public/unveiling.html docs/AGENT-HANDSHAKE.md docs/QA.md`, `npx eslint src --max-warnings=0`, and `npx next build` on Next 16.2.10, 56 routes, passed. |
| Nonso guided tour | PASS | `public/unveiling.html` now presents nine guided chapters around live same-origin routes instead of recreated product sheets or fake CRM figures. The tour embeds `/`, `/mosaic-tiles`, `/piece/aqua-turquoise-blends`, `/visualizer?piece=aqua-turquoise-blends`, `/gallery`, `/admin`, and `/contact`; the Office chapter keeps the CRM honest by loading the real `/admin` path, which redirected to `/admin/login` when unauthenticated. Chrome CDP visual QA on localhost confirmed the desktop shell renders 9 chapters, 1 hotspot, no horizontal overflow, and a 772 by 666 live frame; the visualizer chapter switched the iframe to `/visualizer?piece=aqua-turquoise-blends`; night plus Lagoon changed both the tour and the iframe theme; phone QA at 390 by 844 opened the Piece chapter with the iframe on `/piece/aqua-turquoise-blends`, frame 334 by 506, bottom 814, and no horizontal overflow. Screenshots saved to `/tmp/au-mosaic-qa/unveiling-desktop.png`, `/tmp/au-mosaic-qa/unveiling-visualizer.png`, and `/tmp/au-mosaic-qa/unveiling-mobile.png`. Source scan found no em or en dash glyphs, no off-ramp `font-size` values, and no broad `data-*` selector handlers. `git diff --check -- public/unveiling.html docs/AGENT-HANDSHAKE.md docs/QA.md`, inline script syntax extraction, `npx eslint src --max-warnings=0`, and `npx next build` on Next 16.2.10, 56 routes, passed. |
| Image quality | PASS | 12113234 (aged pool floor, caught by owner) replaced by 28287770, picked by the owner's eye, verified 1400x1867 and loading. All live images return 200; every rendered `img` has alt text. Trimmed 5 unused image URLs and the dead GALLERY export. |
| Accessibility | PASS | Skip link, `:focus-visible` gold rings, alt text on every image, aria labels on menu and float, AA contrast, reduced motion, semantic landmarks (`header`, `main#main`, `nav`, `footer`). |
| Performance | PASS | Every route static or SSG (24 routes). `next/image` with explicit `sizes`; `priority` on heroes only. No webfonts, no animation libraries, no client JS beyond header, theme, reveal. |
| SEO | PASS | Per-page metadata, OG and Twitter cards, JSON-LD LocalBusiness, `sitemap.xml` (all pages and pieces), `robots.txt`, canonical host via `metadataBase`. |
| Analytics | PASS | `@vercel/analytics` wired in the root layout. Fulfils the proposal's traffic analytics deliverable. |
| Copy protocol | PASS | Swept: no em dashes, no arrows, few words, human prose. |
| Visualizer surface pass | PASS | `VISUALIZER_SAMPLE` owns the generated empty pool shell pair, so `/visualizer` opens on `visualizer-empty-pool-day.jpg` by default with pool blues and a starter quad over the bare floor. User photos are chosen, not silently restored, so the first view always explains the feature. The visualizer now has surface fit modes for pool floor, feature wall, backsplash, shower wall, and room floor, plus starter contexts for empty pool, blank wall, kitchen, shower, and open floor. The added stills are `visualizer-feature-wall.jpg` 255K, `visualizer-backsplash.jpg` 244K, `visualizer-shower-wall.jpg` 237K, and `visualizer-room-floor.jpg` 316K. `/pools` keeps the Before water proof frame with the same day and night pair and a quiet link back to the visualizer. Browser smoke on localhost confirmed `/visualizer` renders a canvas immediately, exposes Surface fit and Start from, and loads Blank wall as a separate starter without route change. `npx tsc --noEmit`, `npx eslint src --max-warnings=0`, `python3 scripts/theme-check.py`, `git diff --check`, glyph sweep, and `npx next build` passed. |
| Order record action polish | PASS | Order records no longer keep the add-line form in the page body. `OrderLineAction` now shares the same `useAdminSurface` path as payment and return: desktop swaps the context rail, tablet uses the centered `AdminSheet`, and phone uses the bottom sheet through the compact action. The record body reads first as status, billed, paid, balance, lines, and payments. The shell action chooses Add line for an empty order, Add payment for an owed order, then delivery, receipt, or invoice. The local browser at `/admin/orders` had no order links, so no business data was created for a visual record smoke. The dynamic record was covered by source and type evidence instead. The repo dash scan is now empty, including the client letter. `npx eslint src --max-warnings=0`, `npx tsc --noEmit`, `python3 scripts/theme-check.py`, `git diff --check`, dash scan, and `npm run build` on Next 16.2.10, 56 routes. |
| Deliveries and Owed surface pass | PASS | Deliveries now publish New delivery through `ADMIN_ACTION_INTENTS.deliveryCreate` when open orders exist, with desktop swapping the context rail and compact screens using `AdminSheet`. When there are no open orders, the shell action points to Orders and the page stays quiet. Owed no longer shows the accountant CSV in the body; the export moves to the context action when debts exist, while the reminder action remains the primary path. In-app browser smoke on localhost confirmed `/admin/deliveries` had action `Orders`, no main forms, and no create heading in the empty state; `/admin/debts` had no stale `CSV for the accountant` copy in the quiet state. Source audit confirmed `delivery-create`, `DeliveryCreateAction`, `showDeliveryCreatePanel`, and `data-admin-context-action` wiring. |
| Customer motion and export sweep | PASS | Customer records keep New order as the main route action, while Add motion now lives in context: desktop swaps the rail through `customer-motion`, and compact screens use the shared `AdminSheet`. The sales-motion list stays in the record body, but the add form no longer renders there. Orders now matches Owed: the CSV export moved from the filter row into `data-admin-context-action` as Export orders. Local browser smoke found no local customer record to open, so no fake person was created; the pass was covered by source, type, and route evidence. Focused audit found no stale `motion-kind`, `motion-date`, `motion-note`, `CSV for the accountant` in Orders or Owed pages, `data-event`, old media events, popover, dropdown, More rooms, or `RoomIcon` in admin sources. `npx tsc --noEmit`, `npx eslint src --max-warnings=0`, `python3 scripts/theme-check.py`, `git diff --check`, and the dash sweep all passed. |
| Public gallery media room | PASS | `/gallery` draws 119 owned frames from the registered image library and product plates, with 24 frames on first paint and infinite loading after that. Frame click now opens a Radix photo viewer while the caption keeps one quiet story action. Temporary Chrome smoke at 390 by 844: first frame left 0, right 390, width 390; first title `The Dusk Villa`; no visible `The Dusk Villa by night`; viewer rect 390 by 844 with `overflow-y:auto`; close button 44px at top right; close returns dialog count 0. Desktop smoke at 1280 by 800: viewer rect 1120 by 656 at left 80 and top 72, grid columns 820px and 300px, close returns dialog count 0. `npx eslint src --max-warnings=0`, `npx tsc --noEmit`, `python3 scripts/theme-check.py`, `npx next build` on Next 16.2.10, 56 routes. |
| Desk shell goal | PASS | `docs/DESK-SHELL.md` defines the adaptive admin target before code: room rail, work canvas, context rail, phone sheet behavior, owner-language guardrails, use-case coverage, and Apple HIG source anchors. |
| Desk shell build | PASS | Shared room model, desktop rail, tablet room row, phone tab bar, wide context rail, and mobile context disclosure shipped. Production visual QA at 390, 768, 1024, and 1440 found no horizontal overflow or tab overlap. `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Two-hand cleanup | PASS | Cleared stale staged inverse changes, kept Claude's rail-foot fix, wired Claude's room icons into the live nav, and retired unused `.desk-*` shell primitives. Playwright visual pass at 1440 and 390 found no nav overflow; icons render at 20px; `rg "desk-" src` is empty; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Rail icon binding hotfix | PASS | `AdminNav` now resolves room icons through `RoomGlyph`, removing the stale `RoomIcon` rail reference. `rg "RoomIcon" src/components/AdminNav.tsx` is empty; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Stock filter disclosure | PASS | Stock keeps All, Tiles, Materials, Running low, and Filter on the first screen; colour, place, and order move into the filter panel. The panel uses a stronger blurred surface and phone scrim. Authenticated first paint no longer ships old application-filter links; served CSS includes `filter-surface` and `backdrop-filter`; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Shell standards eye-gate | PASS | The live shell read against `docs/DESK-SHELL.md`, the static half. Owner language stays shop floor: the photo room shows Prepared set, Draft, Approved, Live, product display, and room example, while batch, blob, schema, migration, canonical, insert, and wire live only in code and URL params, never on screen. The gold singleton holds: the rail, the tab bar, and the context rail carry no `btn-gold`, so one gold stays on each room's canvas, and gold text is only the house affordance micro-label. The chrome keeps the type ramp (11, 12, 14, 20, 26) with no border, ring, divide, or hairline. The 1024 to 1279 context seam is closed: mobile context stays until `xl`, where the context rail takes over. |
| Admin glass navigation | PASS | Phone navigation is now the fixed Stock, Orders, People glass island, with only the active label inline. More rooms live in a separate sheet. The gold capsule sits beside the island as an action, never as a tab. Order records publish their state to it: Add payment, Arrange delivery, Send receipt, or The invoice. Desktop rail sits in its own floating glass island. Layer map is explicit in `globals.css`: admin chrome 60, scrims 70, sheets 80, tours 86 to 90, consequence dialogs 99 to 100. `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Quiet failure language | PASS | Home now says "The book is quiet" and asks the owner to refresh or ask for help. The admin error boundary shows a repair note with room and reference only; raw error messages stay in logs, and Try again uses the Next 16 documented retry prop. Focused owner-string audit found no DATABASE_URL, hosting setup, command, database, or engineer language in Home, error boundary, AdminContext, AdminNav, or AdminSheet owner copy. |
| Liquid Glass shell pass | PASS | Local Apple Liquid Glass references stay ignored. The admin rail, compact room island, More sheet, filter sheet, and room context now use a grouped glass material with backdrop blur, saturation, inner highlights, and reduced-transparency fallback. Tablet no longer renders the full room list; Stock, Orders, and People stay in the compact island until the desktop rail takes over. The filter scrim is lighter and blurrier so product cards remain visible beneath it. The admin service worker now fetches build assets from network first and bumps its cache, so old chunks cannot pair with new admin HTML. `node --check public/admin-sw.js`; `rg "RoomIcon|<script|script>"` on touched UI files is empty; no em dash was added; `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Compact action and sheet pass | PASS | Phone navigation now holds Stock, Orders, People, and Photos with no More control. The right action is icon-only, and Add or New actions use the plus glyph. Stock Filter opens as one modal sheet at every size, with scrim 88 and sheet 96 above admin chrome 60 and below consequence dialogs 99 to 100. Compact page-level Add and New links hide below desktop; form submits stay visible. The rail footer uses a beam toggle, and palette active state glows instead of using ring utilities. The Landmarks sample and `docs/LIQUID-GLASS.md` remain ignored. `rg "ADMIN_MORE_ROOMS|IconMore|moreOpen|More rooms|Close rooms|admin-action-icon|sm:absolute|popover|dropdown|ring-"` on touched shell files is empty; `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Stock filter rail swap | PASS | Wide Stock filters now render inside the existing context rail instead of spawning a separate floating sheet. Compact screens keep the simple bottom sheet. One shared `StockFilterPanel` powers both paths, and `admin-context-panel-store` swaps the rail back to room context on Close, Clear, a filter pick, or leaving Stock. `rg "stock-filter-panel|showStockFilterPanel|subscribeAdminContextPanel|xl:hidden|filter-surface|sm:absolute|popover|dropdown|More rooms|IconMore"` confirms the rail path and no returned desktop popover. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Radix sheet and photo edit pass | PASS | Added exactly one direct dependency, `@radix-ui/react-dialog`, and one house primitive, `AdminSheet`. Compact Stock filter now uses `AdminSheet` while desktop Stock keeps the context rail swap. Photo cards no longer open native details: a plain click opens the editor in the context rail at `xl`, in an `AdminSheet` below `xl`, and the same link falls back to `/admin/media/[id]` with Back access. Liquid Glass blur dropped to 30px, with softer daylight shadow and quieter top-left glow. The Landmarks sample and `docs/LIQUID-GLASS.md` remain ignored. `npm ls @radix-ui/react-dialog`; `git diff --check`; `python3 scripts/theme-check.py`; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Compact sheet stack and Photos add pass | PASS | `AdminSheet` now mounts one isolated portal root: root 96, scrim 0, content 1 inside the same stack, so the scrim cannot cover the sheet. While any sheet is open, compact admin chrome fades and stops hit testing, leaving one active surface. The Photos page no longer shows the add form in compact flow: the desktop form parent is `display:none` at phone width, the plus action points at `#media-add-photo`, and it dispatches `admin:media-add-photo` into a Radix sheet. In-app browser pass at 390 by 844 confirmed the add sheet visible above the scrim, with nav opacity 0. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Desktop Photos rail add pass | PASS | The Photos canvas no longer renders the add-photo form at any width. Desktop keeps the page quiet until the context rail action dispatches `admin:media-add-photo`, then swaps the rail to a one-column `media-create` form with `idPrefix="media-rail"`. Compact keeps the same bottom action and opens `AdminSheet` with `idPrefix="media-sheet"`. In-app browser pass at 1440 by 900 confirmed `main #add-photo` absent before and after, with `aside #media-add-photo` plus `#media-rail-title` present after the rail action. A clean compact pass at 430 by 860 confirmed no main form, sheet root z-index 96, and compact chrome opacity 0 with pointer events disabled. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Bottom sheet anchor pass | PASS | The filter sheet was not floating by intent: `liquid-glass` set `position: relative` after Tailwind utilities, so `absolute bottom-0` on `AdminSheet` lost. `AdminSheet` now owns absolute bottom placement in the sheet CSS itself, after the glass material. Compact sheet height drops to `min(72svh, 38rem)` and the tablet floating-card rules are gone, so Stock filter, Photos add, and Photos edit read as true bottom sheets below `xl`. In-app browser pass at 768 by 1151 confirmed the Stock filter sheet is `position:absolute`, full width, `bottomGap: 0`, top at 543px, and root z-index 96. A 430 by 860 pass confirmed `bottomGap: 0`, top at 252px, and compact chrome opacity 0 with pointer events disabled. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Tablet modal breakpoint pass | PASS | `AdminSheet` now uses three shell surfaces: phone stays a bottom sheet under 640px, tablet becomes a centered modal from 640px through 1279px, and desktop keeps the context rail at `xl` and up. In-app browser pass on Stock filter confirmed phone at 430 by 860 has `bottomGap: 0`, full width, and top-only radius; tablet at 768 by 1151 has width 496px, full 28px radius, `centerOffset: 0`, and `bottomGap: 240`; desktop at 1280 has no sheet root and the context rail visible. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 49 routes. |
| Back room surface sweep | PASS | Stock and Photos were used as the pattern and every back room was scanned for inline side-effect work, forms, details, and owner-language leaks. Prepared-photo setup left the Photos canvas: the quiet action now swaps the desktop context rail through `media-batch` and uses the shared `AdminSheet` below `xl`; the server-rendered `/admin/media` page has no `main [data-tour="media-batch"]` and no old add-photo heading in `main`. Fresh enquiries, order line/payment/return forms, and record edit forms stayed where they are because they are the room or record job. Settings, Insights, History, Stock, and Photos no longer render setup commands or hosting language. Authenticated curl checks on `/admin/media`, `/admin/settings`, `/admin/settings/history`, `/admin/insights`, and `/admin/pieces` were clean for `npm run`, `db:push`, `db:seed`, `Vercel`, `Environment Variables`, and `generate-vapid`. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 50 routes. |
| Interaction surface foundation | PASS | Side actions now route through `useAdminSurface`: Stock filter, Photos add, and Prepared photos share one desktop-rail versus AdminSheet decision instead of local matchMedia branches. Photo edit is a full record page again, so the Photos list link goes straight to `/admin/media/[id]` and the context rail no longer carries `media-edit`. `AdminSheet` is the shared Radix surface for compact side work and the order stock-movement confirmation, with phone bottom sheet and tablet or desktop modal behavior when no context rail is involved. Admin layer values now live as tokens in `globals.css`, with tour and sheet classes using those tokens instead of raw `z-[...]` values. Missing staff-key and notification states no longer return setup commands. Focused source audit found no `media-edit`, duplicated rail width hooks, raw admin overlay z-values, `npm run`, `db:push`, `db:seed`, `Vercel`, `Environment Variables`, or `generate-vapid` in owner admin sources. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` on the current worktree passed on Next 16.2.10. |
| Typed admin action intents | PASS | Photos add and Prepared photos now publish typed `AdminActionIntent` values instead of raw event strings. `AdminNav`, `AdminContext`, `useAdminSurface`, and the hidden page action marker all share `ADMIN_ACTION_INTENTS`, so compact FABs and desktop context rail links open the same surfaces without `data-event` drift. Source audit found no `data-event`, `admin:media`, or `dataset.event` in admin surfaces. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 52 routes. |
| Liquid Glass control tint | PASS | Admin room controls no longer fake selection with a flat shell fill. The rail, compact tab bar, and Stock filter rows share `admin-glass-control`: one clipped glass material with gold tint, soft specular highlight, focus highlight, and press response. The Stock filter row type returns to the house ramp at 14px. Focused source audit found no old `bg-shell text-ink shadow-lift` admin controls, no `data-event`, no `admin:media`, and no raw admin `z-[...]` layers. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 52 routes. |
| Photos canvas action trim | PASS | Prepared-photo work no longer sits as a page-header action in the Photos canvas. `MediaBatchAction` now mounts as a silent surface listener beside the hidden page action marker, while the visible prepared-photo entry lives in the context rail or compact context disclosure and still opens the same rail/sheet through `ADMIN_ACTION_INTENTS.mediaBatch`. The stale `AdminTopNav` export is gone, leaving the desktop rail as the only wide room navigation. Source audit found no `AdminTopNav`, no `data-event`, no `admin:media`, no raw admin `z-[...]`, and no old filled-shell active admin control. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree with 53 routes, including one unrelated site route already present in the tree. |
| Compact page action sweep | PASS | Page-header gold actions now hide below the desktop rail breakpoint, not just below `lg`. Stock, Orders, People, Deliveries, and other rooms keep the visible page action on desktop, while phone and tablet rely on the unified icon action plus `AdminSheet` or route action, so compact surfaces do not show two Add or New affordances. Source audit confirmed the shared `admin-page-action` breakpoint is `max-width: 1279px`, matching the `xl` context-rail seam. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree with 53 routes. |
| Admin type-ramp tightening | PASS | The obvious out-of-ramp admin sizes were removed from the shell-facing room surfaces: form fields returned from 15px to 14px, card and record titles moved from 18, 21, or 22px to 20px, the Owed grand total now uses `text-display-section`, and the tour picker name uses 16px. The colour add control became an icon button instead of a text plus. The sticky piece-save bar now uses `--z-admin-save` through `layer-admin-save` instead of raw `z-30`. Source audit found no `text-[15px]`, `text-[17px]`, `text-[18px]`, `text-[21px]`, `text-[22px]`, `text-[40px]`, or raw `z-[...]` in `src/app/admin/(panel)`, `AdminSheet`, or the touched admin layer CSS. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree with 55 routes. |
| Admin small-type sweep | PASS | The remaining shell-facing admin text now follows the house ramp: link-hair actions and uppercase metadata use 12px, body notes and status copy use 14px, and the tour micro state uses 11px. Transparent divider utilities left the panels instead of pretending to separate without drawing. Source audit found no `text-[9px]`, `text-[10px]`, `text-[13px]`, off-ramp large pixel sizes, raw `z-[...]`, or `divide-y divide-transparent` in `src/app/admin/(panel)`, `AdminSheet`, `AdminContext`, `AdminNav`, and admin layer CSS. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree. |
| Admin invoice type ramp | PASS | The printable invoice now follows the same admin type ramp without losing its paper character: back links and captions use 12px, table body and totals use 14px, line names use 16px, the balance uses 26px, and the closing strip uses 11px. Source audit found no `text-[9px]`, `text-[10px]`, `text-[13px]`, `text-[15px]`, off-ramp large pixel sizes, raw z-index utilities, stale page-action anchors, popover, dropdown, or old More rooms language in admin shell and invoice sources. `git diff --check` clean on touched files; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 55 routes. |
| Tablet shell seam | PASS | The shell handoff is now one breakpoint: under `xl`, tablet keeps the compact room bar, header, footer, mobile context disclosure, and `AdminSheet` modal behavior; at `xl`, the rooms rail and context rail arrive together. The old 1024 to 1279 split-brain state is gone, where the rail could appear while the context rail and page-action rules still behaved compact. Source audit found no `lg:` shell handoff utilities in `src/app/admin/(panel)/layout.tsx` or `AdminNav`; the remaining handoff utilities are all `xl:`. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree. |
| Surface router identity | PASS | The shared `useAdminSurface` router now treats an open desktop rail as current only when both the surface kind and its payload match. Stock filter compares the active query fields, Photos add compares the connected piece options, and Prepared photos stays kind-only because it has no payload. If the rail is open with stale payload under the same kind, the router refreshes the rail instead of leaving old filter state or piece choices visible. `git diff --check` clean; focused eslint on `admin-surface-router.ts` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree. |
| Ranges shell action | PASS | The Ranges room now publishes `New range` through the same hidden page action marker as Photos and order records. Its in-page gold action is `admin-page-action`, so phone and tablet show one compact FAB with the right plus action instead of a visible page button plus the Stock room's fallback `New piece` action. Source audit found no naked `href="/admin/ranges/new"` gold link without `admin-page-action`, and confirmed the marker points to `/admin/ranges/new`. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on the current worktree. |
| Order record action surfaces | PASS | Order records now keep line entry in the canvas while payment and return use the shared admin surface router. Desktop opens the context rail, tablet uses the centered AdminSheet modal, and phone uses the bottom sheet. The compact Add payment action carries `ADMIN_ACTION_INTENTS.orderPayment`; return opens from a quiet link; plain rail forms are single column so the 280px rail stays readable. `git diff --check` clean on touched files; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 55 routes. |
| Shared page action resolver | PASS | The bottom FAB and context rail now read one page-action resolver. `AdminNav` no longer owns a private fallback table, and the stale order fallback now points at `#order-payment` with `ADMIN_ACTION_INTENTS.orderPayment`. Record contexts clear inherited room actions, then show the real next action: order records use the hidden server action marker, customer records route to New order, piece records route to Reorder, and ranges or share pages use their published marker. Focused audit found no `#payment`, `data-event`, raw admin media events, old More rooms labels, popover, dropdown, or raw z-index utilities in the touched shell files. `git diff --check` clean on touched files; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 55 routes. |
| Tour language guardrail | PASS | The guided tour now keeps owner-facing copy in shop-floor language: filter choices open remembered views, and piece cards carry face, stock, price note, and window switch. Focused source audit found no forbidden setup or storage terms in Tour owner strings; the only cross-component match was a non-visible panel key. |
| AdminSheet desktop contract | PASS | `AdminSheet` now defaults to a usable modal at every width. The only sheets that opt into `compactOnly` are Stock filter, Photos add, and Prepared photos, because each has a desktop context-rail twin through `useAdminSurface`; the order stock-movement question stays visible as a true modal on desktop. |
| Share bridge shell action | PASS | `/admin/share` now publishes the real next action to compact chrome: matched shares open a new order for that person, unmatched shares open the new-customer flow, and the in-panel gold action hides below the desktop rail breakpoint. The page no longer falls back to the generic People action on phone and tablet. |
| Admin layer token map | PASS | Admin and glass layering now uses named tokens for material effects, content, controls, sun toggle, sheet root, sheet scrim, and sheet content. The sheet root remains isolated, the scrim stays beneath the content, and focused audit found no raw z-index values in `globals.css` or admin shell sources. |
| Admin photo viewer | PASS | Photos and Stock piece photographs now open through one Radix-backed `AdminPhotoViewer`. Phone opens full viewport; tablet and desktop open a centered modal with a small action panel and quick close. In-app browser evidence: Photos at 390 by 844 measured content 390 by 844 with root z-index 98 and compact nav opacity 0; 768 by 1024 measured a 720 by 800 centered modal; 1440 by 900 measured a 1215 by 756 centered modal; `/admin/pieces/classic-pool-blues` at 390 by 844 measured full viewport with nav hidden. `git diff --check` clean; admin z/type audit clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 55 routes. |
| Photos infinite grid | PASS | The Photos canvas no longer renders the whole photo room at once. `/admin/media` fetches a first page of 24 photos, keeps SQL totals for the room counters, and loads the next 24 through one server action as the reader nears the end. In-app browser evidence at 390 by 844: first paint showed `All 227` with 24 visible photo cards; after a deep scroll, the grid appended to 48 cards and stayed idle. `git diff --check` clean; source z/type audit clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 55 routes. |
| Liquid Glass lucency and tab names | PASS | Admin glass materials now read more like window than wall: shell opacity is lower across the grouped rail, filter surface, and sheet material while daylight keeps stronger legibility. Compact icon tabs now carry explicit accessible names even when the visual label is hidden. `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 56 routes. |
| Admin filter and gallery sweep | PASS | Media, Stock, and Orders now expose one Filter affordance instead of chip walls. Desktop swaps the right context rail, phone opens a bottom sheet, and tablet opens a centered modal. Media and Order filter constants moved out of client sheets into server-safe model files. The Photos canvas keeps add and prepared-photo work out of the page body. Public Gallery now mirrors the media room with one column on phone, two at tablet, and three at desktop, each frame linking to its story or tile room. In-app browser evidence: Media mobile had one filter, no main add-photo form, and bottom sheet `bottomGap: 0`; Media desktop had `#media-filter-panel` and no sheet root; tablet modal centered at 496px; Stock and Orders compact pages had one filter and hidden page actions; Gallery measured one first-row card at 390 and three at 1440. Source and docs dash glyph sweep returned empty; `git diff --check` clean; `python3 scripts/theme-check.py` passed all six houses; `npx eslint src --max-warnings=0`; `npx next build` passed on Next 16.2.10, 56 routes. |

## Step-up pass (SICIS benchmark)

| Pattern | Status | Evidence |
|---|---|---|
| Island navigation | PASS | Fixed glass pill, condenses on scroll, menu expands from the island. Heroes full-bleed beneath it. |
| Hover to reveal and expand | PASS | Environment captions on the image; materials and link rise on hover or focus; always visible on touch. Glass "View the piece" chip on collection cards. |
| Floating CTAs | PASS | Piece bar rises after the hero action leaves the viewport (IntersectionObserver); WhatsApp float site-wide. |
| Cinematic immersion | PASS | Home and piece heroes own the full viewport (min-h-svh). Piece pages add a full-bleed "Seen in" environment scene. Every navigation glides in via app/template.tsx. Zero animation libraries. |

## Geometry and glass pass

| Pattern | Status | Evidence |
|---|---|---|
| Element relationships | PASS | Concentric geometry law: capsules inside islands, squircle panels inside squircle bands, no rectangle ever sits in a rounded parent. btn-gold is a capsule everywhere. |
| No lines | PASS | Hairline token and classes deleted from the codebase. Separation by whitespace, imagery, lucent bands, and inner glow only. |
| Liquid glass and lucency | PASS | .glass (sand 42%, blur 28, saturate 1.6) for island, menu, piece bar; .panel (shell 62%, blur 20) for facts; chips at 32% with blur 18. All luminous, none lined. |
| Subpages at par | PASS | All five subpages open with a cinematic PageHero (full-bleed image, words on it, gold action). Fact triads are lucent panels; tint sections float as inset 40px bands; footer floats as a band. |

## Now tier (from the UX review)

| Change | Status | Evidence |
|---|---|---|
| Type ramp shipped | PASS | 21 sizes collapsed to 9 (11, 12, 14, 16, 20, 26 + three display classes), one display leading, three trackings. Census verified by grep. |
| Motion consistency | PASS | Ken-burns on every PageHero; piece bar now glides out as well as in (transition, not unmount). |
| Native details | PASS | Piece bar and WhatsApp float respect env(safe-area-inset-bottom); active press states on every capsule and icon button. |
| Wayfinding | PASS | /piece/* lights the Mosaic tiles tab, aria-current set; float names itself on hover. |
| Measurement | PASS | wa_tap event on every WhatsApp link with placement source (hero, cta, card, piece-hero, piece-bar, float, nav, menu, footer, close, craft) and path. |

## Cinematic media pass

| Change | Status | Evidence |
|---|---|---|
| Hero film | SHIPPED, EYE CHECK PENDING | Water loop 9507847 (URL captured from the Pexels player) fades in over the villa dusk still; poster is the same frame, so every failure mode shows the still. Owner confirms the loop's look on first visible deploy; swap is one line in FILM. |
| Museum reveal | PASS | Piece heroes hang like lit works: spotlight vignette plus TiltFrame pointer lean (hover devices, reduced-motion safe, overscaled edges). |
| Scroll depth | PASS | Lifestyle and Seen-in scenes drift via CSS animation-timeline: view() where supported; static elsewhere. Zero libraries, zero JS on scroll. |

## Regression check (after the glass, bleed, ramp, and film passes)

| Surface | Status | Evidence |
|---|---|---|
| Glass legibility, dark theme | PASS | Measured over the darkest imagery: ink 12.3:1, dusk 5.3:1 on 42% glass. |
| Glass legibility, light theme | FIXED | 42% light glass over a dark hero left dusk at 3.11:1. Daylight glass now 75%: dusk 5.0:1, ink 12.9:1. Night lucency untouched. |
| Mobile full-bleed overflow | PASS | Every -mx-5 grid sits inside a px-5 container; one column on phones means media equals viewport width exactly, no horizontal scroll. |
| Transform stacking | PASS | Tilt (wrapper), ken-burns (image), glide (hover), and parallax (scenes) each own a different element; nothing double-drives one transform. |
| Overlay hit-testing | PASS | Vignette and gradients are pointer-events-none where content sits above; CTAs and links all reachable. |
| Focus and keyboard | PASS | Gold focus rings and skip link unaffected by the glass pass; piece bar untabbable while hidden. |

## The CRM and CMS upgrade path

Ready, and now mechanical:

- `slug` on every piece is the stable product key a database inherits.
- `src/lib/catalog.ts` is the read path: every page and the sitemap ask
  it, never the file. Its four async functions keep their signatures and
  change their source; the Phase 2 swap touches one module.
- Every enquiry deep link carries the piece name into WhatsApp, and
  wa_tap events record which placements convert, so the dashboard opens
  with real data on day one.
- Still Phase 2, on purpose: the store (KV or Postgres), the editor UI,
  auth, media uploads, and on-demand revalidation. That is the paid
  dashboard, not the free site.

## Imagery ledger · CLOSED, WALL TO WALL OWNED

Every frame and the hero film generated for the house, eye-gated
before shipping, compressed under ~500KB, served from public/media.
Zero stock, zero external image hosts, remotePatterns removed from
next.config. Retired along the way: Pexels 28054849, 28287770,
28408521, 32325318, 10231663, 14579397, 30195980, 20975726, 7031713,
35189678, 6110597, 12113234.

| Key | Serves |
|---|---|
| heroDusk | the hero still: twilight villa, mosaic readable tessera by tessera. Replaced the 720p film by quality gate; the loop lives in git history and re-enters at 1080p+. |
| duskVilla | Dusk Villa env card, pool-mosaic scene, the OG card |
| poolBlues | best-seller card, piece reveal, Pool mosaic tile on home |
| glassJewels | Solid colour glass card, reveal, Glass mosaic tile on home |
| koiMural | the identity frame: Pattern and picture mosaics, Art mosaic tile |
| beetleMural | Custom murals card and reveal |
| borders | Patterned pool borders card and reveal, collection page hero |
| craftHands | Why our surfaces last: the trust story |
| terrace | Infinity Terrace env card, pools hero, bulk-order scene |
| hammam | Private Hammam env card, contact hero, feature-mosaic scene |
| darkBath | Dark Bath env card, glass-mosaic scene |
| villaPalms | the lifestyle breath on home, about hero |
| privatePool | pool materials hero: chrome ladder, brass outlet, quiet proof of equipment |

Nonso's real photography joins through the same gate: drop in
public/media, eye check, wire in `src/lib/images.ts`, whitelist in
.gitignore, ledger row here.

## Scene typography (found and fixed during the daylight work)

| Finding | Status |
|---|---|
| Light theme put dark theme gold (#856A30) on dark scrims, near invisible | FIXED: scenes carry their own vars; night scenes force brass #C2A15C in both themes, 6.9:1 |
| Day frames under night scrims would look like fog at morning | FIXED: scrims flip to ivory morning haze only when the day image is active; ink flips with them. Day: ink 15:1, sub 6:1, gold #7A6128 4.9:1 |
| Night frames in light mode | Keep full night treatment: image, scrim, and type stay together, contrast never depends on which pairs have shipped |

## The brand mark (client's au sign, professionalized)

| Item | Status |
|---|---|
| AuMark | Nonso's au sign in the house metals: theme-token fills so the mark relights itself per theme (brass, stone, ivory at night; deep gold and umber by day). au is the chemical symbol for gold. No period, sized to the wordmark, reads as one name. |
| No double AU | The mark says au; beside it, lowercase sans mosaic in the system stack (SF Pro on Apple), matching his logo type. Aria labels still read AU Mosaic in full. |
| Icons | favicon, icon, apple-icon regenerated from the same bitmap via scripts/brand-icons.py. One mark everywhere. |
| Colours | House blues until Nonso's correction arrives; any brand palette then enters through docs/THEMING.md and the theme gate. |

## Premium parity sweep (every page to the same bar)

| Page | Was | Now |
|---|---|---|
| Pool materials | 18 equipment cards rendered empty 4:5 grey frames, the weakest page on the site | Imageless products render quiet panels: name, variants, note, enquire. No empty frames anywhere. |
| 404 | Default Next error page | Maison 404: "This room does not exist," gold action home, WhatsApp aside, wa_tap tagged. |
| About | Legacy MosaicBand with hardcoded v1 colours, blind to the palette system | The owned craft scene (night and day pair) in a 21:9 band. MosaicBand and MosaicMark now have zero users. |
| Imageless piece reveals | Full-screen TileSheet | Accepted as systematic; upgrades when per-range photography arrives. TileSheet grout stays pale by intent: real sheets have pale grout in any theme. |

## The four houses (palette axis)

| Item | Status |
|---|---|
| Palettes | Maison, Lagoon, Terracotta, Onyx: each with night and day token sets, brass, scene golds, and scrim bases. All 64 text pairs plus 16 scene and button checks pass the extended gate. |
| Hardware follows | Logo tesserae, brass button, skip link, glass, scrims, and scene typography all read tokens, so every house restyles the whole site including the mark, no component edits. |
| Picker | Footer swatch row (radiogroup, labelled, ringed active state), persisted to localStorage, applied before first paint. Default remains Maison. |
| One failure caught | Lagoon day mist shipped at 4.30 on first draft; the gate refused it, darkened to 4.5+. The gate works. |

## External lockup review, adjudicated

A second opinion on the logo lockup, checked against the code rather
than the screenshot it was judging.

| Claim | Verdict |
|---|---|
| Wordmark is 700 to 800 weight | Disproved: it ships at 600 (font-semibold). |
| Wordmark is pure white, warm it to #F3EFE7 | Disproved: it ships as ink #F3EFE6, one digit from the reviewer's own suggestion. |
| Switch to Geist or SF Pro | Rejected on law: the stack already serves SF Pro on Apple; adding Geist means a webfont download, and the house ships none. |
| Icon 52px, container 88px, gap 14px | Rejected on architecture: the nav is a floating island, not an 88px bar; those numbers belong to a different layout. |
| Gap too wide | Disproved: it was 6px, tighter than the reviewer's own 12 to 16 range. Raised to 8px for breath. |
| Optical alignment off, drop the word 2 to 4px | Adopted: eye test confirmed the word floated high. Baseline now meets the mark's bottom row, word nudged 2px. |
| Tighten kerning 1 to 2 percent | Adopted: tracking moved from -2.5 to -1.5 percent. |
| The au icon is the strongest part, keep it | Agreed. Untouched. |

Two adopted, one partially, four disproved or rejected with reasons.
The eye test (three rendered variants) settled alignment, not opinion.

## The theme gate (scripts/theme-check.py)

The contrast matrix is now a script, not a ritual: it reads the token
blocks in globals.css and fails any palette under AA before it ships.
On its first run it caught two borderline pairs in our own palette:
night mist on shell at 4.37 (mist now #8A8172, 5.12 on sand, 4.82 on
shell) and night scene brass over a bright image patch at 2.99 (night
scrim bottoms deepened to 0.84 hero, 0.82 card; brass now 3.22 worst
case). Every future palette, including Nonso's brand colours, enters
through docs/THEMING.md and this gate.

## Default flipped to daylight (client feedback)

Nonso reviewed and asked for a lighter look, so daylight is now the
default and night is one tap away. Served HTML, server snapshots,
theme color, and the pre-paint script all flipped together; a saved
night preference still wins before paint. The logo request is parked
until a logo asset exists.

## Instagram brand bridge

| Item | Status |
|---|---|
| Source harvest | PASS. Public `@aumosaic` profile checked on 2026-07-04: AU MOSAIC, Building Materials, No. 1 mosaic tiles in Nigeria, four bio services, Telegram samples link, 96 posts, 1,216 followers, four highlights. Owner screenshot and CRM harvest cross-checked. |
| Data layer | PASS. `src/lib/brand.ts` holds the Instagram facts; `src/lib/site.ts`, `src/lib/facts.ts`, and JSON-LD read the address, channels, and brand claim from stable sources. |
| Site mapping | PASS. Home hero now carries the Instagram claim, a four-pillar profile band routes each bio service, materials include gold and silver mosaic, contact groups WhatsApp, Instagram, and Telegram, footer exposes the same channels. |
| Product language | PASS. Catalogue copy now names Small Seed, Big Seed, Deep Blue, Light Blue, Mixed Blue, Aqua Colour, Crystal, Stone, Chess Board, Gold, Silver, and Rose Gold mirror without adding speculative new stock. |
| Brand dossier | PASS. `docs/BRAND.md` now records profile facts, logo anatomy, colour and type cues, highlight map, visible first-grid media, product taxonomy, sales flow, database caveat, open questions, and design warnings. |
| Address hygiene | PASS. New seeds write the full Shop 17A address; `getFacts` treats old generic Agric Market values as placeholders and falls back to the fuller profile address. |
| Verification | PASS. `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 39 generated routes. Copy sweep found no em dashes or imported Instagram emoji in edited source and docs. |

## Product image ledger batch 02

| Item | Status |
|---|---|
| Extended stock harvest | PASS. Logged-out Instagram still exposed only the public profile slice, direct post HTTP returned shell HTML, and oEmbed returned no captions. Search-indexed Instagram result snippets added second-tier evidence for Tiny Seed Gold, Silver Crystal, pool colour variants, White and Black seed stock, Stone Mosaic, Hexagon Marble Mosaic, and pool construction or renovation. |
| Draft image pair set | PASS. Six new day and night pairs generated into `public/media/product-ledger-2026-07-04/`: tiny seed gold stock, silver crystal, pool colour family, stone interior, hexagon marble bath, and pool construction consultation. All are 1122 by 1402 and remain unwired. |
| Review surface | PASS. `contact-sheet-batch-02.png` eye-checks the new pairs. No source images were taken from Instagram, and no generated draft was referenced from `src/lib/images.ts`. |
| Verification | PASS. Forbidden dash and arrow glyph sweep on edited docs; `git diff --check`; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 39 generated routes. |

## Safari screenshot harvest

| Item | Status |
|---|---|
| Source quality | PASS. Owner Safari screenshots from a logged-in profile view exposed a deeper grid slice than logged-out web fetches, including visible alt text and caption fragments. |
| Inventory update | PASS. Product image ledger now records showroom breadth, over 200 samples, Plain Blue, Mixed Blue Big Seed, Plain White, Orange, deeper Gold and Crystal families, kitchen use, bathroom use, custom artwork, pool motifs, pool materials quote flow, construction, renovation, water fountains, exterior use, and 4 x 8 blue building tiles. |
| Image queue | PASS. Next recommended pairs are showroom sample wall, gold stock variants, crystal stock variants, custom mosaic art and pool motif, then kitchen backsplash and sink. No live media references changed. |

## Product image ledger batch 03

| Item | Status |
|---|---|
| Direction correction | PASS. Batch 03 returns to product display: upright SKU board, flat sheet, loose tesserae, and soft showroom shelves. No lifestyle scenes. |
| Draft image pair set | PASS. Six new day and night stock pairs generated into `public/media/product-ledger-2026-07-04/`: Plain Blue Small Seed, Mixed Blue Big Seed, Plain White, Orange, Black, and Green. All are 1122 by 1402 and remain unwired. |
| Review surface | PASS. `contact-sheet-batch-03-stock.png` eye-checks the new stock display pairs. No source images were taken from Instagram, and no generated draft was referenced from `src/lib/images.ts`. |
| Verification | PASS. Forbidden dash and arrow glyph sweep on edited docs; `git diff --check`; `npx eslint src --max-warnings=0`; `npx next build` on Next 16.2.10, 39 generated routes. |

## The visualizer and the projects gallery

| Feature | Status |
|---|---|
| See it in your space | LIVE at /visualizer. Photo upload or sample pool, four draggable brass stones define the surface, true homography warp (subdivided triangles, no libraries), the piece's colourway laid with the photo's own light (multiply plus soft-light), tile size and grout and blend controls, all 11 pieces selectable. Ends in WhatsApp: Web Share with the composite file where supported, download plus prefilled chat elsewhere. viz_photo, viz_piece, viz_adjust, viz_share, viz_download events. Entry from every photo piece page and deep-linkable with ?piece=. |
| Projects gallery | LIVE at /projects plus four project pages. Concept studies composed from owned frames, labelled Concept study on card and hero, so the owner sees the format his real jobs will fill. Story, scope panel, frame gallery, materials linking back into the collection, WhatsApp close. Projects joined the catalog read path and the sitemap; nav gains Projects. |
| Honesty rule | Concept studies never claim clients or dates. Real projects swap in through the same Project shape, concept flag off. |
| Nav correction | Projects left the island; the nav stays five single words. The work surfaces through story instead: a two-card strip on the home page and two footer Explore lines (Projects, See it in your space). |
| Apple-grade polish | The loupe: while a corner stone is dragged, a 2.5x magnifier with brass crosshair rides above the finger, so precision survives the fingertip. Press and hold the photo (off the stones) to see the original; stones and lines fade while held, an Original chip confirms, release re-renders. Every re-render breathes with one soft light sweep (reduced-motion safe). Haptic ticks on stone grab and release, sliders, piece and grout changes where the platform supports vibration. The session is remembered: photo (downscaled), quad, piece, and controls restore from localStorage on return, ?piece= still wins over memory. viz_compare and viz_resume joined the events. |
| Piece rail correction | The active chip's ring broke the no-borders law; it is gone. Chosen now means risen: elevated surface, ink text, a 4 percent scale, shadow-lift. The rail gained breathing padding so scaled chips never clip, bleeds edge to edge on mobile, and hides its scrollbar entirely (no-scrollbar utility). |

## The back office begins

| Item | Status |
|---|---|
| Analytics | Already complete before the dashboard asked: @vercel/analytics wired in the layout with the /next import, wa_tap placements and the viz_* family tracking custom events. Data flows once the deployment is visited. |
| Environment hygiene | Real Neon credentials arrived in .env.example; moved to .env (mode 600, gitignored, never committed). .env.example is now a clean placeholder template and ships with the repo via a gitignore negation. |
| Database foundation | Neon Postgres plus Drizzle, per the Phase 5 spec. src/db/schema.ts models the two families: catalogue (ranges, pieces, stock) slug-keyed to match the public site's stable keys, and ledger (customers, enquiries, orders with list-versus-given price per line, payments, deliveries) uuid-keyed. Money is integer kobo. src/db/index.ts is a lazy client so the static flagship builds with no DATABASE_URL. drizzle.config.ts prefers the unpooled URL for DDL. Scripts: db:generate, db:push, db:studio. Build stays 30 routes; nothing public imports the db yet. |
| Connection probe | Blocked from the sandbox (network allowlist), as expected. First `npm run db:push` from the owner's machine creates the tables and proves the pipe. |
| Pipe proven | db:push from the owner's Mac (Node 24 via nvm): schema pulled, changes applied, nine tables live on Neon. |
| The door | /admin/login, one password field, Maison voice. HMAC-signed session cookie (AUTH_SECRET), timing-safe password check, httpOnly, 30 days, scoped to /admin. No auth dependency added. Guard lives on the (panel) route group so the login page cannot loop. Sign out is a server action. ADMIN_PASSWORD refuses its own placeholder: auth stays closed until a real password is set. |
| The morning glance | /admin (force-dynamic) reads five live numbers: pieces, stock warnings, open orders, outstanding balance (billed minus paid on open orders), new enquiries. Database unreachable renders a calm panel, and the public site is untouched either way. robots disallows /admin; the panel layout adds noindex. |
| Seed path | scripts/seed.ts (db:seed, tsx) upserts the flagship's own catalogue into ranges, pieces, and zeroed stock rows by slug. Idempotent. Day one of the CRM starts with every piece the site already sells. |
| Phase 2 agreed | Nonso approved the dashboard engagement. The CRM build is funded and underway. |
| The stockroom | /admin/pieces: every piece grouped by range, colour tiles, sheets in stock, Running low and Off the site states visible at a glance, phone-first cards. Empty state teaches db:seed. |
| The piece editor | /admin/pieces/[slug]: one form, one Save. Shop-floor labels (sheets in stock, warn me at, container lands), live colour preview, show-on-site toggle, price note defaults to Quote per job. One server action saves words plus stock together; every action re-checks the session because server actions are public endpoints; hex codes are validated; the save button answers back (Saved / error in Maison voice). revalidatePath refreshes list, editor, and morning glance. |
| Three rooms in parallel | Customers, Orders, and Debts plus Deliveries were built by three agents at once under the CRM.md guardrails, in disjoint directories, no shared-file touches (verified by git status). All fourteen admin routes compiled and linted clean on the first integration build. The guardrails did their job. |
| Customers | List with no-JS search (GET form, ilike on name and phone), new-customer form, and the customer record: their orders with billed and balance in naira, their enquiries, WhatsApp them via waChat, edit form. Malformed uuids 404 instead of 500. |
| Orders | The pipeline grouped by status, settled tucked away with a count. The order record is the centerpiece: five chip-solid steps, lines as stacked panels with List beside Given and the gap named in gold ("₦X below list"), billed sum, payments, serif Balance. Add line (piece select or free description, naira inputs, given defaults to list), record payment, move the step. No line deletes: "Wrong line? Add a corrected one; nothing is ever lost." |
| Who owes what | Read-only ledger. Owing orders grouped by customer, oldest debt first, grand total across everyone, per-order links, WhatsApp reminder prefilled in shop voice. Empty state: "Nobody owes the house. Enjoy it." |
| Deliveries | Grouped Waiting to go, On the road, Landed (last 10). One-step-forward status moves validated against the database (a stale screen refuses calmly), deliveredAt stamped on landing. New delivery hangs off an open order. |
| chip-solid correction | The orders agent caught a real cascade hazard: chip-glass hard-codes white ink in unlayered CSS and beats layered utilities, and it was designed for imagery, not panels. New chip-solid primitive (theme-aware, is-on state, shadow-lift when risen) replaced chip-glass everywhere in the back office, including the pieces list shipped earlier. |
| The hallway | Morning-glance stat cards now open their rooms (outstanding opens Who owes what). The sixth card is the room directory. Header nav stays terse: Pieces, Orders, Customers (desktop), Sign out. |
| Own clothes | The owner asked why the admin wore the island, the footer, and the WhatsApp float: because the root layout dressed everyone. Public chrome moved into a (site) route group with its own layout (island, footer, float, skip link, JSON-LD); the root keeps only html, theme script, and analytics. The back office now renders pure, URLs unchanged. |
| Wayfinding, HIG | AdminNav: on the desk, a quiet top row where the current room holds ink and aria-current; on the phone, a glass tab bar under the thumb (Home, Stock, Orders, People, Owed) with a gold dot on the active room and safe-area padding. Subpages keep their one back link. The owner should never have to ask where he is. |
| Book and window | The distinction the owner asked to see: the book (CRM) holds everything he stocks; the site is the shop window showing only what he puts in it. The stockroom is titled The book, ranges count "X in the book, Y in the window", and the new-piece form says "Put it in the window now" with drafts as the default. |
| Ranges CRUD | /admin/ranges: the shelves. Create (slug minted once from the first name, then fixed because pieces hang off it), edit name, line, and shelf position, see each shelf's pieces. A range shows in the window only when it holds a published piece. |
| New piece | /admin/pieces/new: birth certificate only (name, shelf, line, optional colours); slug minted unique (suffix -2, -3 on collision); stock row created at zero; enters as a draft unless the window switch is flipped; lands on its record page to grow. |
| The photographs | The missing images CRUD, found: a Photographs panel at the top of every piece record. Two slots, night and day, matching how the flagship renders both suns. Upload through a server action to Vercel Blob (8MB cap, type checked, calm "photo store not connected" until BLOB_READ_WRITE_TOKEN is set), thumbnail answers, Take it down clears the record but leaves the file in the store: nothing is ever lost. next.config gains the blob host and an 8MB action body. |
| One canonical home | NEXT_PUBLIC_URL drives SITE.url (localhost in dev, the domain in Vercel); the visualizer share line derives its host from SITE. The old domain left the codebase. |
| A real PWA | Not a bookmark: public/admin-sw.js, scope /admin only so the shop window never runs it. Network-first navigations (a back office is never served stale), cache-first for immutable build assets, and a precached calm offline room (/admin/offline, static, no auth, "The book needs a connection."). Registered by AdminSw in the admin root layout; with the scoped manifest this makes the office installable as its own app. |
| Apple dashboard checklist | Audited the back office against the house philosophy. Passing: one gold button per screen, shop-floor voice, empty states that teach, sentence feedback on every form, phone-first cards, tab-bar wayfinding with aria-current, book-versus-window language, no borders anywhere, chip-solid theme-aware states. Corrected in this pass: morning-glance stat numerals were 34px (off the ramp), now 26px serif like every other big number; (panel) gains error.tsx (a calm "That did not go through. Try again." instead of a stack trace) and loading.tsx (rooms open with a breath: quiet pulse panels, aria-busy). |
| The gaps, closed | Everything named in the audit shipped. Password field: an eye (inline SVG, aria-pressed) so a long password typed on a phone can be checked before Enter. The Owed room carries a quiet gold count of owing customers on both navs, computed per request in the panel layout and staying home if the database is quiet. Orders gained the customers-style no-JS search by customer name with its own teaching empty state. Order status is optimistic: the chip moves the instant he saves (useOptimistic), the server confirms behind it, failure walks it back with a sentence. |
| The funnel remembers | Every WhatsApp tap on the site now becomes an enquiry in the book: WaTracker sends a beacon (never delaying the chat) to /api/enquiry, which stores source, page, and the piece when the page was a piece, no personal data, always answers 204 so the funnel never feels the back office. The People room opens with "Fresh from the window": each tap with its piece and time, cleared with Replied or Close, nothing deleted. The morning glance's New enquiries number is now fed by real taps. |
| Colours by eye | Hex text fields left the piece forms. ColorsField renders the colourway as native colour tiles: tap to open the phone's own wheel, add and remove before Save, hidden comma-joined field so the server action never changed. Native swatch dressed in house geometry (color-dot). |
| The office footer | A quiet strip at the bottom of every panel page: the same PalettePicker (four houses) and ThemeToggle (two suns) as the shop window, sharing one theme store and one localStorage, so the back office wears whatever clothes the owner chose and remembers them everywhere. |
| The second family | The owner caught the miss: the book held mosaics only, and pool materials are half the business. ranges.family (mosaic or pool) and pieces.unit (sheets, bags, units) join the schema (migration 0001); the seed loads POOL_MATERIALS as pool-family ranges with minted slugs, counted in units. The stockroom now reads in two tiers, The tiles and The pool materials; range forms choose the side of the business; the piece editor counts stock in its own unit ("Counted in"). Orders could already line any piece; now every pump and bag of gum cement is a piece. Owner runs db:push then db:seed to apply. |
| Insights | /admin/insights, read-only, business data not traffic: billed month by month (CSS bars, no chart library), what sells the house (top five by revenue), the discount leak as one number with its warning sentence, debt aging in three buckets linking to Who owes what, where the WhatsApp taps come from by source, and stock pressure naming what is running low. Every figure from orders, payments, enquiries, and stock. Insights joins the rooms in both navs. |
| Settings | /admin/settings on a settings key-value table (migration 0002), seeded once from site.ts and never overwritten by reseeds (his edits win). WhatsApp number (normalised to 234), phone display, hours, location, Instagram. Honest copy: the site reads these when the seam flips; until then edits wait safely. The password deliberately does not live here (Vercel env). Settings link sits in the office footer and the hallway. |
| The standards list | docs/SAAS-QA.md: a long modern SaaS admin checklist with honest verdicts, indexed in the doc tree. First mechanics pass shipped against it: one Pager primitive (links, so the URL remembers), customers paginated at 24 with a total count in the intro line, settled orders paginated, and orders gained status filter chips (All open plus the four steps, chip-solid is-on, URL-carried) with a teaching line when a step stands empty. |
| The way back | One Back primitive, HIG manners: a chevron that names where it goes, full 44px thumb target, leans into the walk on hover, aria-labelled. Every subpage opens with it (piece record, new piece, ranges and range record, order record, new order, settled shelf, customer record, new customer). The bare text back-links retired; lateral links stay link-hair. |
| Nav diet | Six tabs were one too many. Insights left the main navs and became Home's second face: a Today and Insights chip pair tops both pages, so the glance and the longer story live in one room. Five tabs again. |
| Scan, not read | Owner's law accepted: people scan. Room intros cut to one short line each (the book: "Everything you stock. The window shows only what you choose."; orders: "List sits beside given. A discount is a number, not a feeling."; customers: "Orders, balance, chat: one tap."; insights: "The business, not the traffic."). Teaching stays where it earns its length: empty states and error rooms. |
| Production hiccup traced | The calm error room appeared on the deployed admin because db:push had not run (the hash-comment accident), so Stock queried a family column that did not exist yet. The boundary behaved exactly as designed. Fix is the two bare commands; production shares the Neon database, no redeploy needed. |
| The Feel sprint, Next slice | Doherty and decision layer, first half: Insights now speaks inference in plain words (last full month against the one before, and "If the pace holds: ₦X this month" from the trailing pace) under the month bars. Open loops per Zeigarnik: enquiry cards carry "No lines yet", the stockroom counts what waits off the site. Search unified on type=search. The glance gains its own refresh line ("Updated just now / HH:MM · refresh") because the installed app hides the browser reload. Still queued from Next: optimistic deliveries and enquiry clears, focus-taking sentences, last-three-touched, the icon set, the unsaved guard. |
| The Feel sprint | All seven Now items from the feel list, one pass. Physics: chips and gold compress under the finger, hairlines dim, haptic ticks on every mutation (save, status, delivery step, enquiry clear, filters). The piece record's Save rides in a glass bar above the tab bar, in thumb reach, desk unaffected. Skeletons mirror their rooms (list shape, piece-record shape, order-record shape), fade in at 250ms so fast loads never flash, slow sweep, reduced-motion safe. Tabular numerals across the whole office: every naira aligns. The stockroom grows filters: desktop chip row (All, Tiles, Materials, Running low, four hue dots bucketed from each piece's first colour) and on the phone a glass bottom sheet with big rows; all URL-carried with a "Nothing wears that filter" way out. The designated-CTA law lands: the glance gains New order, and the oldest debt wears the ledger's one gold ("Send the oldest reminder"). |
| Lagoon daylight by default | The app now opens in Lagoon light: html ships data-palette="lagoon", the pre-paint script honours saved choices (choosing Maison removes the attribute, since Maison is the bare root), theme-store's server snapshot says lagoon, and themeColor plus the admin manifest wear Lagoon's day sand (#eef6f3). The multi-palette contrast gate re-run: every house still clears the matrix. Visitors who already picked a house keep it. |
| The Feel list, Next closed | The queued half of Next, one pass. Deliveries and enquiry clears go optimistic like the status chips: the step reads as taken under the thumb, the server confirms behind, failures walk back with a sentence. Every save answers through one Sentence: it takes focus so VoiceOver hears it, successes fade after four seconds, failures stay; and because React resets a form when its action settles, all panel forms now submit through an onSubmit transition (keep.ts), so a failure never eats what he typed while add-forms still clear on success. Insights gains a gold sparkline over the month bars and an honest Steady or Watch chip on the four panels that carry a state (billed by delta, leak at a tenth of billed, debts by the oldest bucket, stock by warnings). The glance remembers: Where you left off lists the last three records he opened, kept in his phone's localStorage, never the database. The six house icons land (back, eye, filter, close, share, refresh), drawn once in icons.tsx and wired where those verbs already lived; the filter sheet gains a proper close. And the unsaved guard grows its second door: in-app links ask before the router walks, capture-phase, asked once. Lint clean, all routes build. |
| The house asks before stock moves | Item 9. The no-deletes law covers permanence, not physical consequence, so any status move that crosses into or out of Delivered or Settled with piece lines now raises one confirm naming the exact movement: "This takes 20 sheets of Classic pool blues off the shelf. Deliver it?", every line listed with quantity and unit when there are several, the gold carrying the verb (Deliver it, Settle it, Move it back), Not yet staying, Escape staying too. The optimistic chip moves only after the yes, so the screen never lies even for a frame. Moves that touch no shelf keep their one tap: confirms are for consequence, never ceremony. The Consequence and confirmation audit joins SAAS-QA with honest verdicts, payments and hand-typed stock edits accepted without ceremony and the reasons written. Lint clean, all routes build. |
| The tour learns depth | Item 8, the owner's review answered: v1 was too superficial, and inventory needed hands. Tour.tsx rebuilt as chapters, still zero dependencies. The basics keeps the seven cross-room steps; The stockroom deeply walks 28: families, the desk filters with hue dots and sorts, the phone sheet opened and closed by the owner's own thumb, the drafts count, the card truths, then New piece field by field and the whole piece record through photos, words, colours, unit, stock, warn me at, container lands, the window switch, and the sticky Save. Orders deeply ends on Move the step; People deeply closes on the attach. Point steps advance on the gold Next; do steps open a true window in the blocker and advance only when the real element is tapped, capture phase, so hands learn, not eyes. Steps declare their device and their optionality, so the desk never waits for the sheet and an empty book ends kindly. A chapters menu lives under Take the tour, each deep room offers Learn this room, finishing writes aumosaic.toured.<chapter>, and every tour event carries chapter and step. Escape leaves politely everywhere. Lint clean, all routes build. |
| The record's face meets its facts | The owner's second desk pass, aimed where it hurt most: the piece record ran face, words, look, and stock down one narrow column while the desk sat empty. On xl the record now reads the way he thinks about it, a record with a face: the photographs and the window previews hold the left column, the form holds the right, the sticky Save still riding the thumb on the phone, where nothing changed. The share room pairs on the same law: what arrived sits beside who the book found, so the match is one glance instead of a scroll. Both components gave up their own margins and widths to the page grid, the settings lesson applied. Lint clean, 45 routes build. |
| The window's eye | The seam is open, so the owner's camera roll is about to become the flagship's face; this pass makes his photographs arrive as well as the curated set did. Every upload now refines on the phone itself before it travels, with the platform's own canvas and no dependency: HEIC decodes with the codec that wrote it, so the optimizer downstream always receives what it understands and the window can never break on an iPhone shot; orientation bakes in; the long edge caps at 2560; the file re-encodes near 86 quality, so camera weight becomes a few hundred kilobytes before it touches Lagos data. A photograph under 1200px on its long side is refused with a sentence instead of stretched across a full screen. And the record now shows As the window wears it: the uploaded shot inside a true scrimmed hero, eyebrow, serif name, gold close, both suns side by side, so the eye gate happens in the back office and never in front of a customer. The composition itself stays human, which is the point: the tools measure, the eye decides. Shot guidance rides the Teach line and retires with the tour. Lint clean, 45 routes build. |
| The shipping audit | Three parallel auditors walked the whole house: security and actions, domain and data, client and accessibility. Verified clean, with evidence: all 25 server actions check the door on their first line, redirects live outside every try, tokens cannot be forged (role reads from the signed payload only), no SQL reaches the driver unparameterised, money is kobo end to end with balances computed fresh, every catalog write invalidates the tag, heal matches the drizzle files exactly, the cron matches Lagos, and the pagers count what they list. Found and fixed, worst first: the order status write now insists on the status it read (the deliveries pattern), so two thumbs racing the same Delivered can never take stock off the shelf twice; every JSON-LD script walks through scriptJson, which escapes the sequences JSON.stringify leaves open, so a piece named after a script tag cannot break out of one now that names flow from the book; a failed login keeps the typed password (keepValues on the door), and the enquiry row joins the same pattern; the starter re-checks untouched at write time, so racing taps cannot double-load a shelf; the Consequence dialog rises above the tour's blockers; the filter sheet declares itself a dialog; push endpoints cap at 1000 characters; and the tour hands focus back to whoever opened it. Two claims rejected with reasons: hash-then-compare in safeEqual is the standard length-equalisation timingSafeEqual requires, not theatre; and the global enquiry cap stays global by recorded design, because the funnel keeps no addresses to count by. Accepted debts, named: the login rate check-then-log race admits a burst of extra guesses bounded by concurrency, and the tour dialogs trust Escape and aria-modal without a full focus trap. Lint clean, 45 routes build. |
| The tour stops sticking | The owner reported deep tours freezing mid-walk; the diagnosis found the selectors innocent (all 43 chapter targets exist, verified by census) and the engine guilty on three edges. One: an optional do step that timed out ended the whole chapter as done, so a fresh book with no orders killed Orders-deeply at step four, stamped it Seen, and let Teach retire the room's copy on the strength of a failure. Two: required steps after a navigating tap had 5.6 seconds of patience, less than a cold serverless page sometimes needs. Three: the do listener bound to one found node, so a re-render could leave it on a ghost and the tour waiting forever. All three fixed: any optional step now skips on a miss; a lost walk ends as tour_lost with chapter and step tracked, marking nothing Seen and retiring no teaching; patience rises to 8.4 seconds for anything that may follow a navigation; and do taps match at the document against the selector, immune to re-renders. Failure now has an address in analytics instead of wearing done's clothes. Lint clean, 45 routes build. |
| Teaching copy learns to retire | The owner asked for a less wordy app and offered an icon library; the house answered with its own laws instead. Icons stay at the six house-drawn verbs, because typography is the identity and the dependency law has bent exactly once, for web-push; if a seventh verb ever earns a glyph it will be drawn in the house stroke, not imported by the thousand. The words themselves got the real fix: a Teach gate that shows explanatory copy until the room's tour chapter is done, then retires it for good. Words for the newcomer, silence for the resident, exactly the attention root. Ten chronic explainers now retire across the rooms: the enquiry strip's instruction, the corrected-line reminder, the photograph slots' paragraph, both window-switch sublines, three Settings captions, the deliveries and debts intro tails, and the notify caption with its iPhone caveat. Empty states and error rooms keep their teaching forever, by law 4; brand one-liners stay because they are voice, not manual. The server renders Teach as silence, so veterans never see the training wheels flash. Lint clean, 45 routes build. |
| The shell learns the desk | The owner's eye on large screens, answered room by room. The panel layout is now a flex column with the children stretching, so a short or empty room holds the footer at the floor instead of letting it drift to mid-screen. Settings folds into two columns on the desk: the facts lead left, the key rack, the morning tap, and the history stack right, ending the half-empty screen and the long scroll. The order record pairs the two losses on xl, lines facing payments side by side with Where it stands across the top, law 6 as layout. The customer record pairs the person with their history, details left, orders and enquiries right, while the phone keeps its single file everywhere. Deliveries and the debtors both go two-up on the desk, and the history ledger breathes a column wider. Lint clean, 45 routes build. |
| The shop is handed over stocked | The owner's onboarding law: never hand him an empty shop; the guide needs a full cycle of load, sell down, and reload. Two truths landed. First, warn-me-at zero now means he never asked to be warned, fixed at all six judgement sites (the glance count, the stockroom flag, filter, and sort, insights, the digest, and the crossing push), because the freshly seeded book was reading every shelf as Running low and opening on twenty-three false alarms. Second, the stockroom offers Load starting counts while most shelves are untouched: one tap fills only rows still at zero-and-zero with deterministic, believable numbers by family (mosaic in dozens of sheets at warn-me-at 24, pool gear in single digits at 2), audited as stocked the shelves to start, impossible to overwrite a real count, and the strip retires itself once the shop lives. From there the cycle is real data end to end: correct a count, sell it down through delivered with the Consequence dialog naming the movement, watch the crossing tap the phone, reload the shelf. Lint clean, 45 routes build. |
| The seam flips: one organism | Law 10, the finale the boards saved for last. catalog.ts now reads the book: ranges and published pieces map to the window's own shapes under one tag-cached fetch, and every stockroom save calls updateTag (Next 16 deprecated bare revalidateTag; updateTag gives the owner read-your-own-writes, so the window he checks after saving is already true). The repo's catalogue stands permanently behind the book: database silence or an unseeded book falls back whole, which the sandbox build proved by rendering every site page from the fallback with Neon unreachable. When the book answers, the book is the whole truth: an unpublished piece stays gone, never resurrected by the fallback. The visualizer joined the law (pieces arrive from the page via the catalog, not a static import), the footer and contact page read the settings facts, and variants remain curated repo garnish until the book learns a column. And deploys now heal their own schema: instrumentation probes one sentinel at boot and applies pending DDL idempotently, racing cold starts tolerated, so db:push retires as an errand; future schema passes append their statements to src/db/heal.ts beside the drizzle file. Lint clean, 45 routes build. |
| The exclusive design reaches the phone | The owner's three questions, answered in code. Is it Nonso's? The in-app sheet now wears the exclusive template itself: the five tesserae, serif line items with whispered notes, the balance standing under its eyebrow, the letterspaced house strip; what he prints from his phone over the internet is the premium document, and the CSS serif stack hands him true Didot on the iPhone, finer than the script's fallback. Does he have CRUD? Over the order, fully, and the invoice is nothing but the order read fresh: no second document to edit, so no second place for numbers to disagree; delete stays nonexistent by law 8. Does the theme adapt? The chrome around the sheet follows his sun and house; the paper itself stays day tokens on white by decision, because toner has one theme and a letterhead does not change colour per reader. The Python script remains the studio's tool for envelope-grade paper. Lint clean, 45 routes build. |
| The exclusive document | The owner asked for paper that feels like the maison, printed clean and premium. scripts/invoice.py renders it in the og-card tradition: reportlab on A4, day tokens because toner has one theme, the serif cascade preferring Didot on the Mac and falling to DejaVu elsewhere, money always in a face that owns the naira glyph (checked, not assumed), eyebrows letterspaced like the site's 0.18em, amounts in quiet tabular sans, five tesserae as the only ornament, and not one ruled line: air and weight do the separating. Customer sees given prices only. Runs against the book (--order, full uuid or first eight) or as a design proof (--sample); --png asks poppler for 300dpi paper. Two rendering bugs caught by eye in the loop: reportlab's charSpace persists in the content stream until handed back, and the balance eyebrow now sits above its number instead of colliding with it. Output lands in invoices/, gitignored. |
| Checklists two and three, and the paper room | The owner's remaining stacked checklists, verified in code and written as tables in SAAS-QA. The API contract: /api/enquiry promises 204 always with its caps and flood-shedding; the digest promises 401 without the bearer and a quiet {"sent": false} over a loud error; the exports promise BOM, CRLF, integer naira and a deliberate negative for the accountant's credits; compose promises a 302 for every path; every server action checks the session on line one and answers in sentences. The domain truths: zero database deletes in the tree, balances computed fresh and never stored, kobo integer arithmetic end to end, stock moving only at the door with clamped, audited, both-direction crossings, and one finding fixed: the glance's Outstanding now clamps at zero, so a net overpayment reads as nothing owed instead of a negative naira. The Invoices room opened without a dependency: /admin/invoice/[id] renders the order as paper (given prices only; the gap to list stays the owner's number), white in both suns because toner has one theme, one gold Print or save as PDF, office chrome hidden by the print block in globals. And the onyx house passed its measured half: theme-check clears every pairing (gold on shell 10.48 night, 7.08 day); what remains of feel 18 is literally the eye. Lint clean, 45 routes build. |
| The audit in pixels | The first audit read the code; this one read the screen, every admin room photographed live at the desk width through the owner's own browser. Most rooms passed: settings and share hold their two columns, empty states keep the footer on the floor, one gold per screen everywhere, the funnel already counting its first real visitor. Four cuts from what the pixels showed. The refresh icon stacked on top of its word because the hairline link declared inline-block while the icon asked for flex, so the hairline link itself learned inline-flex and the WhatsApp-them link healed with it. The trail of last-touched rooms sat above the five numbers and shoved them under the fold, but the trail serves the returning hand while the tour card serves the new one, so the tour keeps the top and the trail now waits below the pulse. Stockroom cards learn a third column at the widest desks. And the night photograph, the record's face, traded lazy loading for priority so it paints first, not last; a long chase proved the blank frame in early captures was the camera racing the network, not a defect. Lint clean, 45 routes build. |
| The welcome returns, and the desk breathes | Items 10 and 11 close the board. Settings grows its quietest panel: Begin again clears the toured flags on that device, the first-morning offer meets the glance once more, and the teaching copy comes back with it, client-only, the book untouched. The rooms rise four pixels as they arrive, 200ms with the stagger capped at 60ms, written with the translate property so a finished animation leaves no containing block behind and sticky children keep the viewport; reduced motion is honoured and the flagship's theatre stays outside the door. The photographs hold the eye while the form scrolls, sticky at the widest desks. The longer story rests folded behind its first line and a quiet More, still submitting when closed. Buttons carry the pointer. The rest of the sweep was already true and is now written: every list room warms its serif name to gold on hover, and the gold focus ring has guarded the whole house since the accessibility pass. Zig-zag offsets got a written no: the one long single column left is the history ledger, where rhythm would cost scanning. Lint clean, 45 routes build. |
| The proof learns to stand still | An outside review scored the window 9.2 and asked for trust signals; the house answered with its own law, truth only. The reviewer's borrowed numbers (thirty years, five hundred projects) were declined: nothing is minted to fill a slot. Instead the stat prose buried in the craft section became four stones the eye can rest on, home and about, each one defensible across the counter: ten years in the market, our own line in Foshan, the largest stock on the ground in Lagos, Spanish Kerakoll beneath every install. Slots wait for the founding year, an honest project count, and named work, the day the owner sends facts. The camera got its grammar written down too, docs/client/IMAGE-PROMPTS.md: the house DNA block, two suns per scene, environment and piece and journal prompts, and the one law that generated frames live in the dream lane and never wear the clothes of delivered work. Lint clean, 45 routes build. |
| The shelf loses its dead paper | Three documents retired to git history on the owner's word: the product plan (every phase shipped), the dated UX review (its tiers long landed), and the media brief, whose living halves, the film pipeline, the dimensional still, the daylight-twin recipe, moved into the camera's one home at client/IMAGE-PROMPTS.md. Every pointer followed: DESIGN, THEMING, the images module, the schema comment, and the docs index, which now tells the truth in one table. Nothing repeats; the ledger alone keeps the old names, as a ledger should. |
| The photograph learns to name its killer | The owner's first real upload came back with the catch-all sentence, and the catch was silent: store failure and book failure shared one message and left no log line. Split into two doors: the store refuses in its own sentence, the book refuses in its own, and each failure now writes the true error to the runtime logs before it speaks. The next attempt will name its killer; the guard for a missing store key already had its own sentence and stands. Lint clean, 45 routes build. |
| The rooms learn their sizes | The owner read the rooms card as a visual cog: nine names, one suit, a ladder. The cure is the house's own material, typography. The card's title steps down to an eyebrow and the daily three, the stockroom, orders, customers, take serif at 21 with the gold warmth on hover; the occasional six wrap below as a small-caps cloud whose line breaks give the zig-zag for free. Size now means frequency, the mosaic is typographic, and not one new ornament entered the office. Motion stays as it was: the card rises with the room, names warm, hairlines draw. Lint clean, 45 routes build. |
| The gold takes the corner | Owner's eye: New order sat under the heading, spending a full row of the desk. The glance header becomes title left, the one gold right, ends aligned so the button sits on the refresh line's shelf; the phone wraps it back under the thumb exactly where it lived, and the tour still finds its mark. Lint clean, 45 routes build. |
| Every gold takes its corner | The glance's cut, walked across the board so the owner can rest. The stockroom, the orders, the customers, and the road all compose the same sentence now: title and its one line on the left, the one gold on the right with their ends aligned, and the phone wrapping the button back under the thumb where it always lived. Utility rows keep their own line, the search fields breathe wider for it, and every tour anchor rides its moved node. Debts declined on purpose: its only action is a quiet CSV link, and a corner would shout it. Insights already leads with its two faces. Lint clean, 45 routes build. |
| The royal house opens | The owner found the brand that was there all along: the Instagram the client built himself, a thousand followers deep, wearing blue tesserae and navy flyers while the site wore lagoon teal. The fifth house answers it, measured off his own pixels: navy 1e3e90, royal 2456c4, sky 7fb3e8, pale glass ground, and it becomes the main door, the default palette the window opens in. The gate learned the new house and passed it whole: night mist 6.88 on sand, day gold 5.88, the sky capsule holding its near-black label at 8.54, scenes clear in both suns; the other four houses stand untouched and re-measured. The mark stops borrowing the room's metals: a logo is toner, so the au tesserae now wear the brand blues in every house, matching the sign his customers already know. The real handle joins site.ts, sameAs unlocks itself, and the old placeholder can never outrank it. Owner facts still wanted: the newest shop address and the second phone. Lint clean, 45 routes build. |
| The sign above the door | The owner pressed: match his logo display, and manage the data properly. Both paid. The lockup lands as his flyers set it, tesserae au beside a lowercase serif mosaic in the brand blue, sky at night and royal by day as a logotype outside the text gate, in the island nav and the footer. The app icons and the favicon regenerate in the brand blues on the royal night sand, so the phone icon now matches his Instagram avatar tile for tile, and the og card's eyebrow trades brass for sky. Every fact in the screenshots is harvested into the memory with provenance: both handles, both phones, all three address eras, the flyer triad, the trade names his customers speak, the ENTERPRISES question, the samples-to-you promise. Lint clean, 45 routes build. |
| The book corrects its own handle | The settings row still held the placeholder and the owner's browser would not carry the form (React never claimed it; every submit fell to a native GET), so the correction rides the channel the house already trusts: heal.ts gains one guarded UPDATE, the boot probe asks one more question, and the statement neutralizes itself the moment the row is right. The next deploy writes the real handle into the book at cold start; the facts guard already keeps the placeholder from ever outranking it. Lint clean, 45 routes build. |
| The letters learn their own font | The owner looked closer at the sign than the last pass did: his a and u are not pixel bricks but smooth rounded lowercase letterforms with the mosaic living inside them, tesserae and grout filling soft shapes. The mark is redrawn as he draws it: letter skeletons as thick round-capped strokes, a single-storey a of bowl and stem, a u of two stems on its curve, dressed by a deterministic tesserae pattern in the brand blues over navy grout. The icons rebuild from the same geometry through a letter mask, supersampled, and the phone tile now stands beside his Instagram avatar as the same sign. The owner also called the hero's borrowed flyer voice regressive; the verdict is on record and the restoration waits for his word, since identity is the brand's and the voice is the maison's. Lint clean, 45 routes build. |
| The letters step back | The owner's eye ruled against the redrawn letterforms: the smooth rounded a and u read worse than the pixel tesserae they replaced. Reverted whole, mark and icons together, back to the bitmap sign in the brand blues that already stood beside his avatar; the lockup and the serif word keep their place. The ledger keeps both rows, because the attempt and the verdict are both true. A closer look at his letterform font is promised and the geometry waits in git history for that day. |
| The maison takes its voice back | The owner's verdict executed with a knife, not a hammer. Reverted: the hero's borrowed flyer voice (No. 1 in Nigeria returns to Spaces that begin with water, Ask for samples returns to Begin a project, the eyebrow says Lagos again), the profile-promise pillar section and its chips leave the home page, and the tagline stands back up as The house of mosaic. Kept, because data is not voice: brand.ts and BRAND.md, the trade names and variants in the book, the Shop 17A address, telegram, the contact and footer fact links, the search-plain meta, and the samples line in the WhatsApp openers. CODEX.md now stands at the root so any agent who opens this repo learns the law the hard lesson taught: take the brand's identity, never its flyer voice. Lint clean, 45 routes build. |
| The letters join hands | Working inside the bitmap on the owner's word: his sign ties the a and the u through one shared stroke, so the mark now does the same. The gap column retires, the a's stem and the u's left wall become the same two columns of tiles, and the two bases meet at the joint's foot. Twelve columns instead of fifteen, one ligature instead of two letters standing apart, mirrored in the SVG and the icon script alike, icons regenerated and eye-checked against the avatar. The sampled pixel-perfect mark stays on the board for the day the source file lands. Lint clean, 45 routes build. |
| The sign learns its light | The owner brought the flyer-resolution lockup and three truths appeared that the avatar hid: his tiles carry a diagonal light, deep navy at the bottom-left rising to glass at the top-right; loose tesserae scatter off the a's shoulder like the sign is still assembling; and the wordmark is gradient glass, not flat blue. All three land in the bitmap space he chose to work in: position picks each tile's tone from a six-step ramp with hash jitter breaking the banding, five scattered tiles float in a new margin, the word wears its vertical gradient per sun through background-clip, and the lockup tightens to his gap. SVG and icon script share the same arithmetic; icons regenerated and eye-checked. Still waiting on the source file for the tile-for-tile sample. Lint clean, 45 routes build. |
| The sign finds its two voices | The owner's ruling, and the right one: the blue sign is canonical wherever the company faces the world, and the chrome relights with the house. The mark gains a voice: brand keeps the fixed blues for the favicon, the app tiles, the push badge, and the invoice paper, because toner has one theme; room runs the same gradient arithmetic over the palette tokens, so the header, footer, and office mark follow every house and both suns, and in the Royal default the two voices agree to the tile. The wordmark's gradient goes token-driven the same way. And the physical shop sign taught one more letterform truth: the u's foot kicks right past its stem, so the grid grew its thirteenth column. Icons regenerated canonical; the print lockup's lowercase serif stays the digital lockup. Lint clean, 45 routes build. |
| The stroke learns to curve | The owner cut two things with one look: the scattered tiles before the a leave (a flourish his sign never asked for), and the letterforms earn their curves at the lower ends. The a's bowl now rounds at its bottom left, the base stepping in beneath the side wall, and the u gains the tail his artwork has carried all along: the stroke dips below the baseline and hooks left, one grid row deeper. A first attempt read as a numeral 4 and was caught by the eye before it shipped; the tail moved from under the stem to under the bowl and the false foot at the base's end retired. Both renderers share the grid; icons regenerated. Lint clean, 45 routes build. |
| Baselines married | The owner named the greatest task: align the word to the sign. The print lockup rules it, baselines married and not centers: the word mosaic has no descenders so its bottom edge is the baseline, the mark now stands on that line taller than the word's x-height, and the tail row dips below it by exactly its eighth, the way his artwork dips. items-end replaces items-center, the mark grows to 1.45em against the word's 1.4, and the translate carries the tail under the line. Header and footer inherit through the one lockup. His flat render is still waiting to land as a file; the alignment is ready to receive it. Lint clean, 45 routes build. |
| The owner's sign takes the house | His file landed and everything follows it. Masters filed in assets/brand, the 3d render kept for the dream lane; the served derivative trimmed past the whisper-alpha, quantized from 238KB to 41KB. The icon set regenerates from his art on the royal night canvas and the phone tile is now his avatar in truth, not in tribute. The paper carries his file because toner has one theme. And the chrome obeys his ruling: Royal wears the file, the other houses relight the token mark, both rendered and picked by CSS on the palette attribute before first paint, flashless. The lockup marries his mark to the word on the baseline law already shipped. The freeze lifts; the sign above the door is finally the owner's own hand. Lint clean, 45 routes build. |
| The lockup obeys the spec, measured | The owner wrote the lockup law in three clauses and the file was measured to meet them. One: the mark stands 1.45 times the word's x-height, so it drops from looming over the line box to 0.95em against the 1.4em serif, rising just past the ascenders as the print does. Two: baselines married for real this time, the file's own baseline found by scanning the u's stem columns, 95.8% of its height, so a 0.04em drop lands the stem on the word's line and only the tail kisses below. Three: the corridor between sign and word closes to a breath, 0.1em. The tightened box stops the sign inflating the island row, which is what let the lockup drift against the nav items. Lint clean, 45 routes build. |
| Half a tile more air | Almost there, said the owner's eye, and the last fault was the breath: at 0.1em the u kissed the m and the lockup read as one word. The gap opens to 0.18em, the width of about half a tessera at header size, matching the print's tiny but clear light between sign and word. One number changed, nothing else touched. Lint clean, 45 routes build. |
| One word, two materials | The owner's eye again, and again it was right: at render size the sign still outweighed the serif, and the overweight is what kept dragging the lockup off the navbar's middle. The mark drops to the wordmark's own letter height, 0.7em against the 1.4em serif, so au and mosaic read as one word set in two materials, tiles then type, and the lockup's tight box finally centres true against the nav items. The baseline drop scales with it. Lint clean, 45 routes build. |
| The sign rises to its seat | The owner's next turn of the dials: the mark was hanging below the word's line like a subscript. It grows by his eighth, 0.7em to 0.8em, and the seat swings from a 0.03em drop to a 0.05em lift, so the au rides the word's x-band instead of dangling under the baseline. Two numbers, one look. Lint clean, 45 routes build. |
| The descender pit, found by arithmetic | Not quite there, said the eye, for the third time, so the dials gave way to font metrics. The house serif on his machine is Didot, and Didot's line box carries a descender zone near 0.19em below the baseline at the word's size: the invisible pit every small nudge failed to climb, which is why the sign kept reading as a subscript. Computed seat: the mark at 1.45 times Didot's x-height, 0.91em, lifted 0.15em, the pit depth less the file's own overshoot. The lesson for the ledger: when optical alignment resists three nudges, stop nudging and read the font's numbers. Lint clean, 45 routes build. |
| The word grows a little, the seat holds | Finally tracks, said the owner, and asked one more turn: the wordmark up a touch. The serif rises 1.4em to 1.5em and the mark's height and lift ride the same ratio, 0.97em seated at minus 0.16, so the alignment won by arithmetic does not reopen. The lockup is settled: his file, his proportions, the descender pit mapped and paid. Lint clean, 45 routes build. |
| Another turn of the word | The wordmark rises again, 1.5em to 1.6em, mark and lift riding the locked ratio at 1.04em seated minus 0.17. The seat holds by construction now: every future size turn is one word-size number and the arithmetic follows. Lint clean, 45 routes build. |
| Every surface answers to the sign | The alignment sweep the owner called: everything that says this is the house now points at his logo. The PWA manifest grounds on royal night with royal day chrome. The og card retires its typed name and carries his mark in the shadowed band under the sky eyebrow. The structured data gains the logo property beside sameAs, so search engines learn the sign the customers know. And the exclusive paper joins the language: the mark rides at the wordmark's letter height beside the serif, baselines married by the same lockup law, while the five-tesserae ornament trades the old metals for the sign's own blues. Icons were already his file; the room voice already relights the other houses. One identity, every door. Lint clean, 45 routes build. |
| One sign in every house | The owner closed the last identity question: the other palettes adopt his logo too, because a logo does not change clothes with the room. The two-voice system retires whole: the token pixel-mark, its room ramp, its gradient arithmetic, and the palette-switching CSS all leave for git history, and AuSign becomes one image in every house and both suns. Terracotta, Lagoon, Maison, and Onyx now show the same blue sign his customers know from Instagram, the shop board, and the paper. The component shrank by eighty lines and the brand grew singular. Lint clean, 45 routes build. |
| The sign carries its own ground | The owner caught the leak in the one-sign law: the wordmark still borrowed the room's gold in other houses. Now the whole sign is blue by decree, the word wearing the brand gradients per sun and never per palette, and the smart half of his idea seals it: the lockup rides in a quiet pill of the palette's own sand, night or day, so the blue sign always stands on a neutral ground it can read against, in Maison amber or Onyx stone alike. The safe-area move every real logo ships with. Lint clean, 45 routes build. |
| The badge keeps its ground | Light mode left the pill pale and the sign looked adrift beside the nav. The owner's cure, made systemic: the pill now wears the palette's own night sand in both suns, through the mode-invariant triplet every house already defines, so daylight shows a dark themed badge, royal navy here, umber in Terracotta, near-black in Onyx, and the logo sits on the ground it was born on, the avatar's. One gradient now serves the word everywhere, since the ground never lightens. Lint clean, 45 routes build. |
| The badge becomes night glass | First draft flat, second draft material. The owner asked for intention and got the fused treatment he chose from four: the pill is now dark liquid glass, blur and saturation like the island it sits in, tinted by a diagonal fade of the palette's night sand that rises toward the top-right, the same direction the logo's own tiles brighten, with a breath of sky at the corner and the glass recipe's inset lights finishing the edges. One material with the nav, one light source with the sign, themed by every house's triplet. Lint clean, 45 routes build. |
| The glass goes lucent | More window, less wall: the pill's tint drops from near-solid to the island's own lucency, 0.68 falling to 0.42 across the diagonal, and the blur rises to the house glass numbers, 28 and 1.6, so whatever scrolls beneath melts and flows through the night tint instead of stopping at it. The inset lights brighten a breath to keep the edge. Lint clean, 45 routes build. |
| The capsule dies; the sign stands bare | Three drafts of pill polish and the owner called it: we were sucking, and the senior answer was subtraction. Diagnosis on the record: a dark translucent capsule over a light island can only make mud, physics not tuning; the island already is the sign's ground; the logo's own navy outline is its safe-area device, the reason the print sits straight on white and the avatar straight on black; and gradient-clipped words die at chrome size. The pill and its night glass leave for git history. The word wears solid brand ink per sun, royal navy by day as the print, pale glass at night as the avatar; the gradient voice stays with the og card and the paper where it has room to fall. Trust the sign. Lint clean, 45 routes build. |
| The harvest starts to hang | Sixty-eight generated frames sat local and the owner asked what they were for. The answer runs in three lanes, per the image ledger the other session wrote: winners wire now, SKU stock waits on Nonso's word, mood drafts stay drafts. First winners wired by content swap on canonical names: the metallic mirror trio replaces the old gold accents scene and the crystal-stone worktable replaces the old custom colours scene, both suns, eye-gated on a proof sheet, compressed to house weight, no code touched because the names never moved. The stock pairs for Small Seed, Big Seed, White, Orange, Black, Green, tiny-seed gold, and silver crystal stand ready in the draft folder behind the owner questions already in the memory. Lint clean, 45 routes build. |
| The green light stocks the window | All green from Nonso, and the gate lifts in one pass. Ten trade-name SKUs from his own Instagram language become real pieces: two blues by seed size, tiny seed gold, silver crystal, plain white, black, green, orange, stone, and hexagon marble, each with its eye-gated night and day pair compressed to house weight, whitelisted, wired through images.ts and products.ts, and inserted into the production book by the heal channel, insert-only with a probe sentinel, so no existing row and no uploaded photograph can be touched. Counts and warn-at start at zero because the shelves are the owner's to fill; the window shows the new pieces the moment the deploy boots. Lint clean, 45 routes build. |
| The next frontier is planned, not built | The owner set the target in one breath: shop.app's product-as-object, Apple's reveal ceremony, the maison's museum cinematography, and colour houses with attitude, grounded in Lagos because specificity is the premium. The plan lands on the board as items 12 and 13. The piece reveal: four acts, stage, material, dream, counter, prototyped on tiny-seed-gold before any rollout, the owner's eye as the gate. The houses learn attitude: the gold king is Oba, not Arabian, because Lagos already has a king; Royal keeps the throne; characters live in tokens and names, never costume imagery. Nothing shipped in this pass except the plan itself, which is how big things stay small. |
| The reveal opens on tiny seed gold | Item 12, the piece reveal, approved by the owner's eye as an in-chat prototype and wired to the window on one piece, tiny-seed-gold, gated by slug so the pattern proves before it generalises. Four acts sequenced not blended: the stage opens dark under the vignette spotlight, the sheet lit alone on a slow kenburns hold; the material walks a light-sweep across the tesserae, close enough to touch; the dream pulls back into the gold vault, the scene elevated on the house parallax; the counter turns shop-crisp, the colourway dots, the facts, and the one gold Enquire straight to WhatsApp. Built from the house's own primitives, SceneVars, ThemeImage, Reveal, PieceBar, the vignette, the scrims, kenburns, and CSS scroll-driven parallax, with no new dependency. Theatre is lawful on the window, so the stage and material hold dark in both suns while the dream and counter follow the light. Reduced motion collapses every act to stills, act heights are phone-first in svh, and the night white and brass hold AA on the dark acts. Lint clean, types clean, all 49 static pages compiled (the sandbox export cleanup hit a filesystem limit only; Vercel builds clean). |
| The Oba house opens | Item 13, the houses learn attitude, first character built. Oba joins beside the five as the sixth house, Lagos majesty answered in Yoruba rather than gulf pastiche: gold on warm midnight with a coral fire, the king the city already has. One palette block in globals, both suns, nine tokens plus the hardware (brass, the two scene golds, the sand triplets); a swatch in the footer picker seated between Royal and Maison; the name added to the store and to the theme-check gate. Attitude lives in tokens and name only, never costume, so the media stays the shared maison photography per the theming contract, and whether Oba takes Maison's seat as the default is left to the owner. The gate passed both suns on the first pass, every text duty clear: night ink 16.5, dusk 9.0, mist 6.3, and gold 10.8 on sand; the tightest pairs, day gold 4.98 and day scene gold 4.97, still clear 4.5, and the button label reads 10.8 on the brass. The character is written into THEMING.md. Lint clean, types clean, theme-check gate passed with no regression to the other five houses. |
| The reveal's dream takes the gold room | The other session's cinematic window batch gave the reveal its dream. Batch 06's gold frame, a curved gold, silver, and rose-gold tesserae column in a warm Lagos interior, was eye-gated at full size and left the draft folder as canonical metallic-room-night.jpg and metallic-room-day.jpg, compressed under the house still budget and whitelisted. The reveal's third act now hangs that room in place of the older gold vault, both suns through SceneFrame, and reads "The gold takes the room, and holds the evening in it." Only the dream changed: the stage, material, and counter still wear tiny-seed-gold's own photograph, and the swap reverts by pointing the scene back at goldAccents. First wire from the window lane, drafted by the other session and hung by this one. Lint clean, types clean. |
| The window gains five cinematic rooms | The rest of the other session's window batch enters the site, five frames of six (the gold room already hangs in the reveal), each canonical night and day, compressed under the house budget and whitelisted. The homepage cinema grows by two: the Showroom Wall (every colour within reach) and the Pool Edge (where the blue begins) join the Environments grid, which renders straight from the array, so six rooms now alternate down the page. Three heroes trade a borrowed image for the right one: pool materials opens on the materials counter instead of a generic pool, projects opens on the custom art gallery instead of the villa it used to share with about, and about's craft band becomes the artisan table. Two suns each. No product data changed and no piece moved, so the pass reverts by pointing the four usages back at their old constants. Lint clean, types clean. |
| The product plates land | The other session's commerce lane, the shop-style plates it drew from Shop.app and Jeffrey Court, enters the site. Five plates left the draft folder as canonical night and day pairs, the product on a studio ground, warm off-white by day and near-black by night, so they read in both suns. The homepage Materials grid trades its four scene tiles for four family plates, pool blues, glass, gold and silver, and custom art, so the section that names the materials now shows the tile itself with shop clarity instead of a room. The pool-materials shelf gains its own product shot, the pump, tank, ladder, and fittings on one plate hung above the equipment list, so the page the counter hero opened now shows the kit up close. No card architecture changed, so the range grids keep their scene photographs and the plates live only where a clean family tile belongs. On the backend question: no new table was needed, the ranges family enum already carries pool, so pool materials can become stockable records through the same insert-only heal channel whenever the owner wants the shelf managed. Lint clean, types clean. |
| The home shows the room and the sheet | Show more, not replace, made law on the home page. The Materials grid returns to the maison's own rooms, pool water, glass jewels, the gold vault, and the koi mural, the aspirational frames the plates had briefly displaced. Beneath them a new By the sheet strip carries the four commerce plates, so the section shows the space and the sample side by side, the room to dream in and the tile that arrives at the door. The gallery way, made a rule: an audit first confirmed every displaced scene still hung somewhere, so nothing was orphaned, and a review gallery of the whole collection, seventy-five shipped frames and the draft lane, was rendered at tmp/gallery for the eye. Lint clean, types clean. |
| The grid learns Shop discipline | The catalogue takes shop.app's grid lesson, gated by the owner's eye first. Fourteen pieces gain a clean product card: the ten stocked SKUs their exact Batch 07 cards and the four families their Batch 05 plates, each the product alone on a studio ground, warm off-white by day and near-black by night, the camera barely moving so a shopper compares fast. A card field on the product, keyed by slug through one map over both the book and the fallback, feeds the grid only; the piece page still reads image, so every piece keeps its own hero photo and the grid shows the sheet while the page keeps the room. The six originals without a card (aqua, deep-midnight, patterned-borders, gradient, container, custom-colours) hold their scene until one is drawn. No piece page changed and no hero moved. Lint clean, types clean. |
| The reveal opens every door | The answer to one sample piece: every piece page is the four-act reveal now, shop.app's product-detail discipline across the whole catalogue. The component turned data-driven, so one route serves all twenty-one: the stage, material, and counter object is the piece's shop card where one exists, else its own hero photo; the dream in act three is a cinematic window frame chosen by a family map, pool to the pool edge, glass to the showroom wall, feature to the art gallery, bulk to the showroom, with a per-slug override keeping the gold room for the gold pieces; the copy is generic and honest, cut and set by hand, with tiny-seed-gold's bespoke gold lines preserved. The slug gate retired and the old single-piece layout with it. Theatre holds where it belongs: stage and material dark in both suns, dream and counter following the light, reduced motion collapsing every act to stills. Known softness, recorded: the pieces without a shop card show their scene photo as the stage object until a card is drawn. Lint clean, types clean; Vercel promotes only a green build, so a bad prerender cannot reach the window. |
| The silhouette dies in the showroom | The smooth-outline experiment ran as a picture in a folder, never a commit: bitmap to marching squares to Chaikin curves, fine tesserae clipped inside, edge tiles slicing like the print. The owner's eye ruled in three words and the ruling is final: the pixel-grid sign IS the identity. No smooth-silhouette treatment of the mark again; the letterform question is closed unless the client's own source file reopens it. Nothing shipped, nothing reverted, one lesson recorded. |
| The boundary audit, checklist one | The owner's first stacked checklist, run and written. The walk found two missing doors and one scattered truth. The public flagship had no error boundary at all: a client crash (the visualizer being the likeliest) would have shown a customer the machinery; the maison now answers with a calm line, a gold Try again, and the chat that never goes down, while details whisper only to the console. Office 404s used to walk the owner out to the shop window; That page is not in the book now keeps him inside, one gold back to the glance. And rowsOf moved home: one normaliser owned by src/db, imported at every raw execute (insights, panel layout, digest), so no room maps the driver envelope again. Verified in the same pass: redirects and notFound throw outside every try, actions answer in sentences instead of throwing at the UI, the panel layout re-checks the door on every render so Try again cannot swallow an auth redirect, and global-error stays honestly Missing, accepted, until the static shell ever earns one. The audit table lives in SAAS-QA. Lint clean, 44 routes build. |
| The float steps aside on piece pages | The owner's design question, answered by the house's own laws. The WhatsApp float is the one saturated brand-foreign colour in the maison (the palette law says no saturated fills), it competes with the gold singleton (standing rule 19), and it speaks Material where the house speaks HIG. On piece pages it was strictly redundant: the piece bar already carries the same WhatsApp action in house clothes, and the two shared the bottom edge. So it steps aside there, one floating chrome per screen. Everywhere else it keeps its seat for now, because Lagos knows the green circle and law 6 says commerce stays human. The verdict on the rest belongs to evidence: the float signs its taps (data-wa float), so the Where the taps come from panel decides in a few weeks whether it stays, dresses in house clothes, or retires. Lint clean, 44 routes build. |
| The product image ledger opens | The Instagram harvest now has a product-facing ledger at docs/client/PRODUCT-IMAGE-LEDGER.md: pool blue seed samples, metallic mirror trio, chess board mosaic, crystal and stone, pool materials, and sample flow are mapped from social evidence to the site and stockroom. Six Lagos-luxury draft images landed in ignored media under public/media/product-ledger-2026-07-04, plus a contact sheet for eye review. Nothing is wired into src/lib/images.ts yet, so rollback is deleting the ignored folder and reverting the docs. Lint clean, 39 routes build. |
| The product batch learns two suns | The product draft folder now mirrors the app's media law: six day files and six night files, all 4:5, with contact-sheet-pairs.png for review. Night means blue hour and warm near-black, not a dim filter; day means Lagos daylight, not a washed copy. No live code or src/lib/images.ts wiring changed, so the entire image pass still rolls back by deleting the ignored folder and reverting the ledger. Lint clean, 39 routes build. |
| The stock ledgers learn the house camera | The live site was rendered locally at desktop and mobile widths before the final image batch. The audit found that SKU boards were useful proof, but the site wants one calm product moment with air, water, stone, and depth. Batch 04 landed twelve ignored draft images and contact-sheet-batch-04-classic-stock.png: sample library, pool blue decision ledge, metallic range, speciality neutrals, bold colour family, and pool materials quote counter, all in day and night. Nothing was wired into src/lib/images.ts. Rollback is deleting the ignored folder and reverting the product ledger. Lint clean, 39 routes build. |
| Product display learns commerce discipline | Shop.app and Jeffrey Court were audited as product-display references. Batch 05 added twelve ignored draft assets and contact-sheet-batch-05-commerce-display.png for product-card plates: classic pool blues, metallic accents, solid colour glass, speciality neutrals, pool materials kit, and custom mosaic art, each with light and dark display versions. Nothing was wired into src/lib/images.ts. Rollback is deleting the ignored draft files and reverting the product ledger. Lint clean, 39 routes build. |
| The window gets its last cinema batch | Before dropping deeper into Shop-style product display, the window layer got one tight final cinematic set: showroom wall, pool edge, metallic room, artisan table, pool materials counter, and custom art gallery, each in light and dark. The metallic room was retaken after the first pass made figure-like reflections, and the corrected contact-sheet-batch-06-cinematic-window.png now separates site-window atmosphere from product-card inventory. Nothing was wired into src/lib/images.ts. Rollback is deleting the ignored draft files and reverting the product ledger. Lint clean, 39 routes build. |
| The catalogue switches fully to Shop discipline | The next layer is no longer cinematic. Batch 07 added twenty ignored draft assets and contact-sheet-batch-07-shop-sku-cards.png for exact SKU cards: plain blue small seed, mixed blue big seed, plain white, black, green, orange, tiny seed gold, silver crystal, stone, and hexagon marble, each with light and dark versions. The camera barely moves between SKUs, and the product difference comes from tile scale, colour, grout, reflectivity, stone texture, or hex geometry. Nothing was wired into src/lib/images.ts. Rollback is deleting the ignored draft files and reverting the product ledger. Lint clean, 49 routes build. |
| The gaps get their Shop cards | The prepared showroom set added fourteen ignored draft assets and contact-sheet-batch-08-gap-showroom.png: aqua, deep and midnight, patterned borders, mixed gradient, container orders, custom colours, and a kitchen backsplash proof pair. The first six stay in product-card discipline. The kitchen pair is showroom proof only, not a client archive. Nothing was wired into src/lib/images.ts. Rollback is deleting the named prepared files and reverting the product ledger rows. No em dash or arrow in the touched docs, `git diff --check` clean, sample dimensions verified at 1122 by 1402. Lint clean, 49 routes build. |
| The media bench opens | The first backend media room landed without replacing the piece record: `pieces` gained `card_image_night` and `card_image_day`, `media_assets` holds draft, approved, live, archived assets by set, sun, role, linked piece, notes, source, dimensions, and original path, and `/admin/media` gives the owner filters for Draft, Approved, Live, Archived, Product display, Window scene, Showroom photo, Review sheet, and Prepared set. The prepared set was uploaded to Blob and inserted: 15 media rows, 12 card assets live on the six gap pieces, two kitchen frames approved as proof, and the contact sheet left draft. `catalog.ts` now prefers backend card slots, then falls back to `CARD`; `PieceReveal` reads piece card fields too. Lint clean, 49 routes build. |
| The room bridge enters the home | The home page now has one bridge between environments and materials: In the room. The first card reads approved showroom proof rows from `media_assets`, so the kitchen backsplash pair can show as showroom proof without becoming a product card or a client archive. If the book or proof rows are quiet, `catalog.ts` falls back to owned scenes from `src/lib/images.ts`. The other three frames use the pool edge, gold room, and custom wall. Rollback is removing the home band, or moving the two proof rows back to draft so the fallback scene appears. Lint clean, 49 routes build. |
| The photo room speaks shop | The admin room now says Photos, Product display, Room example, Showroom photo, Review sheet, Live, and Prepared set. The backend keeps its exact storage words, but the owner sees showroom language in filters, empty states, action buttons, result sentences, image alt text, and dashboard links. Older upload errors no longer expose storage keys. Lint clean, 49 routes build. |
| The photo room becomes a library | `/admin/media` now has create, update, replace, and archive. New photos upload to Blob as draft media rows, edit forms can relabel use, light, status, piece, and note, Replace swaps the file while keeping the record, and Archive is the delete path by CRM law. Product displays marked Live write the matching day or night card slot on the piece, and moving them away from Live clears only a slot that still points at that exact URL. Prepared photos remain as shortcuts. `/tmp/` is ignored. Lint clean, 49 routes build. |
| The open board gets honest | Documented gaps were separated into shipped code, stale documentation, owner truth, and future builds. The root now has a maison global-error fallback, the db:push errand language is retired in favour of schema healing, shipped brand and image-ledger work no longer reads as future work, and `docs/NEXT-STEPS.md` holds the remaining owner decisions and build priorities. Lint clean, 49 routes build. |
| Returns come back cleanly | Orders now record returns without erasing the sale: a mirrored line points to the original line, cannot exceed what remains, lowers billed value, and restores stock immediately when the order is delivered or settled. Credit leaves the customer in credit; refund writes a negative payment. Invoices and WhatsApp messages speak returns and credit plainly. Migration 0007 plus schema healing add `order_items.return_for_item_id`. Lint clean, 49 routes build. |
| Product trade facts | Product variants are now a piece-level decision: seed size, shade, and finish live on the piece record, appear in the stockroom cards, and feed the public product variant line. Anything needing its own count or price remains a separate piece. Migration 0008 plus schema healing add the fields. |
| Application tags | Kitchen, sink, bathroom, sitting room, office, exterior, swimming pool, pool floor art, logo art, and wall art now live as controlled piece tags. The stockroom can filter by place; product pages show the tags in the counter and structured data. Migration 0009 plus schema healing add the field and starter map. |
| Sample sales motions | Customer records now track showroom visits, sample pictures, site sample visits, pool-size quotes, and materials lists with open or done status, date, note, and audit sentences. Service records stay under owner truth until Nonso confirms the pool-service boundaries. Migration 0010 plus schema healing add the room. |
| Admin pulse query | The morning glance and Owed badge now read one cached raw query through `src/lib/admin-pulse.ts`; the home page no longer spends five separate glance queries plus a layout badge query. |
| Onyx eye pass | The last buildable board item is closed. The photo room and piece photograph panel were checked live in Onyx night: empty and loading photo wells now carry an inward glow, file buttons stay readable capsules, and the window preview's eyebrow plus enquiry chip read from scene and brass tokens instead of fixed Maison gold. `scripts/theme-check.py` passes across all six houses. Lint clean, 49 routes build. |
| Stock moves, and the phone is told | Item 7, the last on the handoff. The domain decision landed as written: crossing into delivered takes each line's quantity off its piece, clamped at zero (never below the visible truth), walking back out returns it, and every movement signs the history per line ("took stock for a delivery · 6 sheets out, 14 left"). The action sentence learned arithmetic: a delivery that pushes a piece across warn-me-at answers "Delivered. {piece} is running low: {qty} {unit} left." The spine is Web Push, the one justified dependency: push_subscriptions (migration 0005, endpoints go inactive, never deleted), VAPID keys in env with .env.example placeholders, sw push and notificationclick handlers that open the room that asked, and Notify this phone in Settings with the iPhone install caveat written where he reads it. The rhythm honours the attention root: one digest at 08:00 Lagos (Vercel Cron, CRON_SECRET guarded) saying only what needs him, "The house is calm" when nothing does, plus true crossings the moment they happen. Nothing else, ever. Lint clean, 44 routes build. |
| Onboarding: the script and the tour | Item 6, both halves. docs/client/VIDEO-SCRIPT.md holds the word-for-word three-minute walkthrough for a WhatsApp video: the glance, one gold New order, list beside given ("the gap is your discount, and now it is a number you can see, not a feeling"), recording a payment, the oldest debt wearing the reminder, a stockroom edit, photos, and the window switch. And the tour shipped hand-rolled, no dependency: seven steps, each a selector, a page, a title, one line; the spotlight is one giant box-shadow that glides with its element and follows it through scroll and resize; a glass card in thumb reach carries dot progress, the gold Next, and a whispering skip; the router walks it across five rooms and home again for the peak-end; Escape leaves politely; VoiceOver hears each step as a dialog. Finishing or waving it away writes aumosaic.toured, which retires the first-login offer on the glance; Take the tour stays permanently in the rooms list; tour_start, step, done, and skip all track. Reduced motion respected, targets that never paint end the tour kindly instead of hanging it. Lint clean, 43 routes build. |
| The SEO sprint, code half | Item 5's buildable half. Titles turned search-plain while every H1 kept house voice: the root default now says what the house sells and where, the range pages carry their intents (mosaic tiles in Lagos, pool materials, pool construction), pieces title as name plus collection plus Lagos. The business card grew honest fields only: url, telephone, image, priceRange, street address, and Instagram joins sameAs the day the real handle lands in site.ts. Piece pages carry Product schema (no invented price, because quotes live in the chat) and BreadcrumbList, absolute URLs throughout. And the Journal opens: five SSG intent guides from PLAN Phase 6 (choosing mosaic, pool tiles, pool construction, pump prices, how a quote is built), each in house voice with a search-plain title, an FAQ answered on the page and in FAQPage schema, a WhatsApp close carrying journal-slug as its data-wa source, and a place in the sitemap plus the footer. No invented numbers anywhere: the guides teach factors and send prices to the chat, where they are true today. Lint clean, 43 routes build. |
| The funnel learns to count people | Item 4 of the handoff: full-flow tracking, first-party only. The site's beacon now carries one anonymous id, minted on the visitor's first tap with crypto.randomUUID and kept in their own localStorage: no fingerprinting, no third parties, nothing before the tap. Enquiries gain session_id (migration 0004), stored only when it reads like a uuid. The desk gains the attach mechanic: a fresh enquiry ties to a customer from an inline picker (Tie them, answered in a sentence), and the loop closes by itself: opening an order marks that customer's open enquiries converted. Insights draws From tap to settled: people who tapped (distinct ids), enquiries, became customers, orders billed, orders settled, with the two honest rates in words; before db:push lands the column, the top row waits gently instead of erroring the room. |
| The WhatsApp bridge | Item 3 of the handoff, both halves. Sharing a chat into the installed app lands in /admin/share (share_target, GET): the room reads any 234 or 0-prefixed number out of the shared text with one normaliser (phone234, now shared with waChat), matches it against the customers, and answers with the person the book knows: gold New order prefilled with them, their record a whisper away, or for a stranger a New customer prefilled with the number; either way one tap keeps the text as a fresh enquiry (source share, nothing parsed twice). The iPhone caveat is written where he reads it, since share_target is Android with Chrome. And compose-from-the-book: Send the quote and Send the receipt on the order record walk through /admin/compose, which reads lines, billed, paid, and balance fresh from the book at that moment, signs the history, and opens the customer's WhatsApp with the message written in house voice. Lint clean, 41 routes build. |
| The missing list closes | All six named gaps in one pass, two commits. Mechanics first: the landed-deliveries archive and fresh enquiries turn pages (active work still shows whole, a page past the end walks back), customers sort Newest or A to Z, the stockroom sorts inside each range (shelf order, by name, low first, desk chips and sheet rows alike), the accountant gets orders.csv and debts.csv (session-guarded GET links, kobo split to naira by integer arithmetic, BOM so Excel reads names right), and the billed panel opens three windows (three months, six, a year, URL-carried, interval from a whitelist never the URL). Then the keys and the ledger: staff and audit_log tables (migration 0003), named staff keys hashed with the house secret, the session cookie names its holder while old cookies still verify as the owner, the key rack in Settings (owner only, keys go inactive, never deleted), every action signs the book's history in sentences, readable and paged at Settings, and the door learns patience: eight refusals in ten minutes rest it, thirty enquiry rows in ten minutes shed the flood, both counted from the book and failing open before db:push. One owner errand: npm run db:push. Lint clean, all routes build, 39 of them now. |
| The back room gives controls to the shell | People and Orders now keep the first glance about the room: no page-body Add customer, New order, inline customer search, or inline order search. Each room gets one filter and search surface through AdminSheet: desktop swaps the right rail, tablet centers a modal, phone rises from the bottom. Customer records publish New order, WhatsApp, Owed, Delivery, and invoice actions to the shell instead of the record body. Order records do the same for payment, return, quote, receipt, invoice, and customer record links while the body keeps status, lines, and payments. Deliveries loses its top New delivery button, and Owed moves the oldest reminder to the room action while card reminders stay quiet links. Browser checked People desktop rail, tablet modal, phone bottom sheet, Orders desktop rail and phone bottom sheet, plus the Deliveries and Owed page action sweep. Local data has no customer or order record to open visually, so those record pages were covered by TypeScript and build. Build clean, lint clean, types clean, theme-check clean, no em dash scan clean, 56 routes build. |
| The gallery stops hiding the photo room | The public gallery now reads the media bench first: 111 approved or live photographs, excluding contact sheets, then the 119-frame curated archive behind them, deduped by source so no photograph has to fight its twin. Product plates, room examples, window scenes, and showroom proof all get public labels, terse lines, and the right path home, with piece-linked media going straight to its piece page. The current Blob host is already covered by the Next image allowlist, and the running gallery route returned the new media titles beside the static archive. Build clean, lint clean, types clean, theme-check clean, diff-check clean, no em dash scan clean, 56 routes build. |
| The art card gets a real work | The homepage audit found the last visible generated colour sheet in the collection grid: Pattern and picture mosaics had a mural hero but no product-card pair, so ProductCard fell back to its math tile sheet. The owner's Instagram Sacred Heart artwork became the evidence, not a copied asset: two new generated product-card photographs show a framed sacred-heart style mosaic panel in the maison camera, night on near-black stone and day on warm ivory stone, compressed under the house still budget and wired through `CARD`. The piece reveal keeps its existing mural scene; only the grid card changes. Build clean, lint clean, types clean, theme-check clean, diff-check clean, no em dash scan clean, 56 routes build. |
| The gallery obeys the window | Public gallery pairs now render as one work with two suns: the grid shows the night photograph in night mode and the day twin in daylight, without repeating the same title as two cards. The media bench still keeps both rows, but its viewer now finds the matching twin, swaps the modal image with the active sun, and keeps direct edit paths for both files. Browser checked `/gallery`: `hexagon-marble-night.jpg` became `hexagon-marble-day.jpg` after the day toggle, with no extra day card. Browser checked `/admin/media`: Window Showroom Wall Light opened as light in day mode, then as the dark twin in night mode, with Edit photo and Edit night photo available. Build clean, lint clean, types clean, theme-check clean, diff-check clean, no em dash scan clean, 56 routes build. |
| The piece page gets its shop counter | Product pages keep the four-act reveal and gain the useful counter below it: product sheet, applied frame, colour dots, trade facts, one WhatsApp action, visualizer handoff, three buying promises, and nearby pieces from the same range. The route passes related pieces through `getPieces`, so the page still composes from the catalogue. Browser checked `/piece/classic-pool-blues` on desktop and phone: no horizontal overflow, product action fits, fixed piece bar stays below the counter action, and the lower page renders Same range. Build clean, lint clean, types clean, theme-check clean, diff-check clean, no em dash scan clean, 56 routes build. |
| Pools starts with the shell | `/pools` now carries the full pool story instead of a small service list: the terrace still opens the dream, the visualizer's empty pool becomes the practical before-water anchor, the services read from `catalog.ts`, the quote stack names shell, surface, circulation, and care, and the finish plus equipment photographs show beauty beside the parts that keep it alive. Materials-only work now has its own counter path using the buying steps, and the final quote section keeps one clear WhatsApp action. Browser checked `/pools` at desktop and phone widths: the empty pool is visible in the first follow-up section, no horizontal overflow, and no console errors. Build clean, lint clean, types clean, theme-check clean, diff-check clean, no em dash scan clean, 56 routes build. |

## The collection, photo complete · both suns

Six remaining tile-sheet pieces became photography in one batch, each
eye-gated as a night and day pair: aqua blends (lamp-lit evening and
midday caustics), midnight blends (readable dark, warm in the
depths), the gradient wall (dusk made of tiles), gold accents (the
vault of the house), container orders (the night before the container
ships), and custom colours (the artisan's table). Every one of the
eleven catalogue pieces now carries owned photography in both themes.
The gate caught one filename mix-up along the way: files named gold
were the gradient wall; the eye wires, not the label.

## Daylight pairs (the theme follows the sun) · COMPLETE

Every night frame has its daylight twin, every twin eye-gated for
same scene, same tiles, believable morning. The toggle is a sunrise
across the entire maison.

| Pair | Status |
|---|---|
| hero-dusk + hero-day | PAIRED. Same villa, same tree, same grid; brass streaks become sky reflections. |
| glass-jewels + glass-day | PAIRED. The jewel case at noon: daylight transmission, ivory room behind. |
| koi-mural + koi-day | PAIRED. Same fish, same swirls, skylight morning; gold flecks glint in daylight. |
| beetle-mural + beetle-day | PAIRED. Iridescence shifts emerald to teal in daylight; gold glints. |
| hammam + hammam-day | PAIRED. The oculus pours morning; wet honey mosaic, no steam, the drip survives. |
| dusk-villa + dusk-villa-day | PAIRED. The same address before breakfast: gold horizon, mosaic through glassy water. |
| terrace + terrace-day | PAIRED. Swimming-hour midday: crisp caustics, white sparkle on the spill, true blue sea. |
| craft-hands + craft-day | PAIRED. Honest work at ten in the morning; the adhesive ridges throw crisp daylight shadows. |
| dark-bath | stays dark in both themes by identity |
| borders, villa-palms, private-pool, pool-blues | already daylight, no pair needed |
